################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# runtime/src/iree/hal/drivers/hip/BUILD.bazel                                 #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(IREE_HAL_DRIVER_HIP)
set(_hip_platform_defines "")
if(IREE_HAL_DRIVER_HIP_RCCL)
  list(APPEND _hip_platform_defines "IREE_HAL_HIP_ENABLE_RCCL=1")
endif()
iree_cc_library(
  NAME
    hip
  HDRS
    "api.h"
  SRCS
    "api.h"
    "cleanup_thread.c"
    "cleanup_thread.h"
    "context_util.h"
    "dispatch_thread.c"
    "dispatch_thread.h"
    "event_pool.c"
    "event_pool.h"
    "event_semaphore.c"
    "event_semaphore.h"
    "graph_command_buffer.c"
    "graph_command_buffer.h"
    "hip_allocator.c"
    "hip_allocator.h"
    "hip_buffer.c"
    "hip_buffer.h"
    "hip_device.c"
    "hip_device.h"
    "hip_driver.c"
    "hip_multi_queue_command_buffer.c"
    "hip_multi_queue_command_buffer.h"
    "memory_pools.c"
    "memory_pools.h"
    "native_executable.c"
    "native_executable.h"
    "nop_executable_cache.c"
    "nop_executable_cache.h"
    "per_device_information.h"
    "rccl_channel.c"
    "rccl_channel.h"
    "stream_command_buffer.c"
    "stream_command_buffer.h"
  DEPS
    ::dynamic_symbols
    iree::async
    iree::async::util::proactor_pool
    iree::base
    iree::base::core_headers
    iree::base::internal
    iree::base::internal::arena
    iree::base::internal::atomic_slist
    iree::base::internal::flatcc::parsing
    iree::base::threading
    iree::base::threading::thread
    iree::hal
    iree::hal::drivers::hip::util::hip_util
    iree::hal::utils::collective_batch
    iree::hal::utils::deferred_command_buffer
    iree::hal::utils::executable_debug_info
    iree::hal::utils::executable_header
    iree::hal::utils::file_transfer
    iree::hal::utils::files
    iree::hal::utils::queue_emulation
    iree::hal::utils::queue_host_call_emulation
    iree::hal::utils::resource_set
    iree::hal::utils::stream_tracing
    iree::schemas::executable_debug_info_c_fbs
    iree::schemas::hip_executable_def_c_fbs
  DEFINES
    ${_hip_platform_defines}
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_HIP)
set(_dynamic_symbols_platform_defines "")
if(IREE_HAL_DRIVER_HIP_RCCL)
  list(APPEND _dynamic_symbols_platform_defines "IREE_HAL_HIP_ENABLE_RCCL=1")
endif()
set(_dynamic_symbols_platform_deps "")
if(IREE_HAL_DRIVER_HIP_RCCL)
  list(APPEND _dynamic_symbols_platform_deps iree::third_party::rccl_headers)
endif()
iree_cc_library(
  NAME
    dynamic_symbols
  HDRS
    "dynamic_symbols.h"
    "hip_headers.h"
    "rccl_dynamic_symbols.h"
    "rccl_status_util.h"
    "status_util.h"
  TEXTUAL_HDRS
    "dynamic_symbol_tables.h"
    "rccl_dynamic_symbol_table.h"
  SRCS
    "dynamic_symbols.c"
    "rccl_dynamic_symbols.c"
    "rccl_headers.h"
    "rccl_status_util.c"
    "status_util.c"
  DEPS
    iree::base
    iree::base::core_headers
    iree::base::internal::dynamic_library
    iree::base::internal::path
    iree::third_party::hip_api_headers
    ${_dynamic_symbols_platform_deps}
  DEFINES
    ${_dynamic_symbols_platform_defines}
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_HIP)
iree_cc_test(
  NAME
    dynamic_symbols_test
  SRCS
    "dynamic_symbols_test.cc"
  DEPS
    ::dynamic_symbols
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.hip"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-hip-tests
)
endif()

if(IREE_HAL_DRIVER_HIP)
iree_cc_test(
  NAME
    graph_command_buffer_test
  SRCS
    "graph_command_buffer_test.cc"
  DEPS
    ::hip
    iree::async
    iree::async::util::proactor_pool
    iree::base
    iree::base::threading
    iree::hal
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.hip"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-hip-tests
)
endif()

if(IREE_HAL_DRIVER_HIP)
iree_cc_test(
  NAME
    hip_allocator_test
  SRCS
    "hip_allocator_test.cc"
  DEPS
    ::dynamic_symbols
    iree::async::util::proactor_pool
    iree::base
    iree::base::threading
    iree::hal
    iree::hal::drivers::hip::registration
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.hip"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-hip-tests
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
