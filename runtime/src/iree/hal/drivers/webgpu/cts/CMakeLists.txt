################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# runtime/src/iree/hal/drivers/webgpu/cts/BUILD.bazel                          #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(IREE_ARCH STREQUAL "wasm_32" AND IREE_HAL_DRIVER_WEBGPU)
iree_wasm_entry(
  NAME
    cts_main
  MAIN
    "webgpu_cts_main.mjs"
  PUBLIC
)
endif()

if(IREE_ARCH STREQUAL "wasm_32" AND IREE_HAL_DRIVER_WEBGPU)
iree_cc_library(
  NAME
    backends
  SRCS
    "backends.cc"
  DEPS
    ::cts_main
    iree::hal
    iree::hal::cts::util::registry
    iree::hal::drivers::webgpu::registration
  TESTONLY
  ALWAYSLINK
  PUBLIC
)
endif()

if(IREE_ARCH STREQUAL "wasm_32" AND IREE_HAL_DRIVER_WEBGPU)
iree_runtime_hal_cts_test_suite(
  BACKENDS
    ::backends
  LABELS
    "iree-build-requirement=runtime.hal.webgpu"
    "iree-run-requirement=runtime.resource.webgpu_device"
    "runtime-resource=webgpu-device"
  RESOURCE_GROUP
    iree-hal-drivers-webgpu-tests
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
