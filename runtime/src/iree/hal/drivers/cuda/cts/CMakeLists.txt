################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# runtime/src/iree/hal/drivers/cuda/cts/BUILD.bazel                            #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(IREE_HAL_DRIVER_CUDA)
iree_cc_library(
  NAME
    backends
  SRCS
    "backends.cc"
  DEPS
    iree::async::util::proactor_pool
    iree::base::threading
    iree::hal
    iree::hal::cts::util::registry
    iree::hal::drivers::cuda::registration
  TESTONLY
  ALWAYSLINK
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_CUDA)
iree_runtime_hal_cts_test_suite(
  BACKENDS
    ::backends
  NAME
    graph
  ARGS
    "--cuda_use_streams=false"
  LABELS
    "iree-build-requirement=runtime.hal.cuda"
    "iree-run-requirement=runtime.resource.nvidia_gpu"
    "runtime-resource=nvidia-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-cuda-tests
)
endif()

if(IREE_HAL_DRIVER_CUDA)
iree_runtime_hal_cts_test_suite(
  BACKENDS
    ::backends
  NAME
    stream
  ARGS
    "--cuda_use_streams=true"
  LABELS
    "iree-build-requirement=runtime.hal.cuda"
    "iree-run-requirement=runtime.resource.nvidia_gpu"
    "runtime-resource=nvidia-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-cuda-tests
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
