# Copyright 2026 The IREE Authors
#
# Licensed under the Apache License v2.0 with LLVM Exceptions.
# See https://llvm.org/LICENSE.txt for license information.
# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

load("//build_tools/bazel:cmake.bzl", "iree_cmake_extra_content")
load("//build_tools/testing:build_defs.bzl", "iree_execution_test_suite")
load("//runtime/build_tools/bazel:cc.bzl", "iree_runtime_cc_binary")

package(
    default_visibility = ["//visibility:private"],
    features = ["layering_check"],
    licenses = ["notice"],  # Apache 2.0
)

iree_cmake_extra_content(
    content = """
if(IREE_ENABLE_THREADING)
""",
    inline = True,
)

iree_runtime_cc_binary(
    name = "iree-dump-cpuinfo",
    srcs = ["iree-dump-cpuinfo.c"],
    visibility = ["//tools:__pkg__"],
    deps = [
        "//runtime/src/iree/base",
        "//runtime/src/iree/base/internal:cpu",
        "//runtime/src/iree/base/tooling:flags",
        "//runtime/src/iree/schemas:cpu_data",
        "//runtime/src/iree/task:api",
    ],
)

iree_execution_test_suite(
    name = "execution_test",
    manifests = ["iree-dump-cpuinfo.test.json"],
    tools = {
        "iree-dump-cpuinfo": "//tools:iree-dump-cpuinfo",
    },
)

iree_cmake_extra_content(
    content = """
endif()
""",
    inline = True,
)
