################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# runtime/src/iree/tools/iree-remote-check/BUILD.bazel                         #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(IREE_HAL_EXECUTABLE_ARTIFACT_AMDGPU)
  if(NOT CMAKE_C_COMPILER_ID STREQUAL "Clang" OR
     NOT CMAKE_SYSTEM_NAME STREQUAL "Linux")
    message(FATAL_ERROR
      "IREE_HAL_EXECUTABLE_ARTIFACT_AMDGPU=ON requires Clang on Linux.")
  endif()
  iree_runtime_configure_amdgpu_toolchain(REQUIRED)
endif()

if(IREE_HAL_EXECUTABLE_ARTIFACT_AMDGPU AND CMAKE_C_COMPILER_ID STREQUAL "Clang" AND CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_amdgpu_binary_variants_embed_data(
  NAME
    amdgpu_artifacts
  TARGET
    amdgcn-amd-amdhsa
  TARGETS
    ${IREE_HAL_AMDGPU_TARGETS}
  BINARY_NAME_PREFIX
    iree_remote_check_kernel
  C_FILE_OUTPUT
    "amdgpu_artifacts.c"
  H_FILE_OUTPUT
    "amdgpu_artifacts.h"
  IDENTIFIER
    "iree_remote_check_amdgpu_artifacts"
  SRCS
    "amdgpu_kernel.c"
  FLATTEN
  PUBLIC
)
endif()

if(IREE_HAL_EXECUTABLE_ARTIFACT_VULKAN)
iree_spirv_asm_module(
  NAME
    vulkan_kernel_artifact
  SRC
    "vulkan_kernel.spvasm"
  TARGET_ENV
    "vulkan1.3"
  C_IDENTIFIER
    "iree_remote_check_vulkan_kernel_artifact"
  PUBLIC
)
endif()

iree_cc_library(
  NAME
    artifact_types
  HDRS
    "artifact_types.h"
  DEPS
    iree::hal
  PUBLIC
)

if(IREE_HAL_EXECUTABLE_ARTIFACT_AMDGPU AND CMAKE_C_COMPILER_ID STREQUAL "Clang" AND CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    amdgpu_artifact
  HDRS
    "amdgpu_artifact.h"
  SRCS
    "amdgpu_artifact.c"
  DEPS
    ::amdgpu_artifacts
    ::artifact_types
    iree::base
    iree::hal
    iree::hal::executable::amdgpu::executable_target
    iree::hal::executable::amdgpu::target_id
  PUBLIC
)
endif()

if(IREE_HAL_EXECUTABLE_ARTIFACT_VULKAN)
iree_cc_library(
  NAME
    vulkan_artifact
  HDRS
    "vulkan_artifact.h"
  SRCS
    "vulkan_artifact.c"
  DEPS
    ::artifact_types
    ::vulkan_kernel_artifact_c
    iree::base
    iree::hal
  PUBLIC
)
endif()

set(_artifact_platform_defines "")
if(IREE_HAL_EXECUTABLE_ARTIFACT_AMDGPU)
  list(APPEND _artifact_platform_defines "IREE_REMOTE_CHECK_HAVE_AMDGPU_ARTIFACT=1")
endif()
if(IREE_HAL_EXECUTABLE_ARTIFACT_VULKAN)
  list(APPEND _artifact_platform_defines "IREE_REMOTE_CHECK_HAVE_VULKAN_ARTIFACT=1")
endif()
set(_artifact_platform_deps "")
if(IREE_HAL_EXECUTABLE_ARTIFACT_AMDGPU)
  list(APPEND _artifact_platform_deps ::amdgpu_artifact)
endif()
if(IREE_HAL_EXECUTABLE_ARTIFACT_VULKAN)
  list(APPEND _artifact_platform_deps ::vulkan_artifact)
endif()
iree_cc_library(
  NAME
    artifact
  HDRS
    "artifact.h"
  SRCS
    "artifact.c"
  DEPS
    ::artifact_types
    iree::base
    iree::hal
    ${_artifact_platform_deps}
  DEFINES
    ${_artifact_platform_defines}
  PUBLIC
)

iree_cc_test(
  NAME
    artifact_test
  SRCS
    "artifact_test.cc"
  DEPS
    ::artifact
    iree::base
    iree::hal
    iree::hal::utils::device_spec_builder
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "manual"
)

iree_cc_binary(
  NAME
    iree-remote-check
  SRCS
    "iree-remote-check-main.c"
  DEPS
    ::artifact
    iree::async
    iree::async::util::proactor_pool
    iree::base
    iree::base::internal::path
    iree::base::threading
    iree::base::tooling::flags
    iree::hal
    iree::hal::remote::client
    iree::hal::remote::client::transport_factory
    iree::net::transport_factory
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
