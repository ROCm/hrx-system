################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# experimental/xdna/BUILD.bazel                                                #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA)
iree_cc_library(
  NAME
    amdf_status
  HDRS
    "amdf_status.h"
  SRCS
    "amdf_status.c"
  DEPS
    amdf::headers
    iree::base
  PUBLIC
)
endif()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA)
iree_cc_test(
  NAME
    amdf_status_test
  SRCS
    "amdf_status_test.cc"
  DEPS
    ::amdf_status
    amdf::headers
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.xdna"
)
endif()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA)
iree_cc_library(
  NAME
    executable
  HDRS
    "executable.h"
  SRCS
    "executable.c"
  DEPS
    iree::base
    iree::hal
    iree::hal::drivers::amd::xdna::image
    iree::hal::drivers::amd::xdna::image::aie2p::native_image
    iree::hal::drivers::amd::xdna::image::aie2p::target
  PUBLIC
)
endif()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA)
iree_cc_test(
  NAME
    executable_test
  SRCS
    "executable_test.cc"
  DEPS
    ::executable
    iree::base
    iree::hal
    iree::hal::drivers::amd::xdna::image::aie2p::npu2
    iree::hal::drivers::amd::xdna::image::testdata::mul_i32
    iree::hal::drivers::amd::xdna::image::testdata::mul_i32_npu4
    iree::hal::drivers::amd::xdna::image::testing::aie2p_image_fixture
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.xdna"
)
endif()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA)
iree_cc_library(
  NAME
    prepared_command
  HDRS
    "prepared_command.h"
  SRCS
    "prepared_command.c"
  DEPS
    ::executable
    amdf::headers
    iree::base
    iree::hal
    iree::hal::drivers::amd::xdna::image::aie2p::transaction
  PUBLIC
)
endif()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA)
iree_cc_test(
  NAME
    prepared_command_test
  SRCS
    "prepared_command_test.cc"
  DEPS
    ::executable
    ::prepared_command
    amdf::headers
    iree::base
    iree::hal
    iree::hal::drivers::amd::xdna::image::aie2p::npu2
    iree::hal::drivers::amd::xdna::image::directory
    iree::hal::drivers::amd::xdna::image::format
    iree::hal::drivers::amd::xdna::image::testdata::mul_i32
    iree::hal::drivers::amd::xdna::image::testing::aie2p_image_fixture
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.xdna"
)
endif()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_execution_test_suite(
  NAME
    cli_test
  MANIFESTS
    "iree-xdna-run.test.json"
  TOOLS
    "iree-xdna-run=::iree-xdna-run"
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.xdna"
)
endif()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_cc_binary(
  NAME
    iree-xdna-run
  SRCS
    "iree-xdna-run.c"
  DEPS
    ::amdf_status
    ::executable
    ::prepared_command
    amdf::amdf_static
    amdf::headers
    iree::base
    iree::base::tooling::flags
    iree::hal
    iree::hal::drivers::amd::xdna::image::aie2p::npu2
    iree::io::file_handle
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
