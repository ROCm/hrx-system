################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libamdf/cts/util/BUILD.bazel                                                 #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    provider_headers
  HDRS
    "provider.h"
  DEPS
    amdf::headers
  TESTONLY
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/..>"
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    device_cache
  HDRS
    "device_cache.h"
  SRCS
    "device_cache.cc"
  DEPS
    ::provider_headers
    amdf::headers
  TESTONLY
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    test_main
  SRCS
    "test_main.cc"
  DEPS
    ::device_cache
    ::provider_headers
    amdf::headers
    iree::third_party::google_test
  TESTONLY
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    static_provider
  SRCS
    "linked_provider.cc"
  DEPS
    ::provider_headers
    amdf::amdf_static
    amdf::headers
  TESTONLY
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    shared_provider
  SRCS
    "linked_provider.cc"
  DEPS
    ::provider_headers
    amdf::amdf
    amdf::headers
  TESTONLY
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    linked_test_main
  DEPS
    ::static_provider
    ::test_main
    amdf::headers
  TESTONLY
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND AMDF_BUILD)
iree_cc_library(
  NAME
    dynamic_provider_posix
  SRCS
    "dynamic_provider_posix.cc"
  DEPS
    ${CMAKE_DL_LIBS}
    ::provider_headers
    amdf::headers
  TESTONLY
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Windows" AND AMDF_BUILD)
iree_cc_library(
  NAME
    dynamic_provider_win32
  SRCS
    "dynamic_provider_win32.cc"
  DEPS
    ::provider_headers
    amdf::headers
  TESTONLY
  PUBLIC
)
endif()

if(AMDF_BUILD)
set(_dynamic_provider_platform_deps "")
if(CMAKE_SYSTEM_NAME STREQUAL "Windows")
  list(APPEND _dynamic_provider_platform_deps ::dynamic_provider_win32)
else()
  list(APPEND _dynamic_provider_platform_deps ::dynamic_provider_posix)
endif()
iree_cc_library(
  NAME
    dynamic_provider
  DEPS
    amdf::headers
    ${_dynamic_provider_platform_deps}
  TESTONLY
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
