################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libamdf/src/platform/BUILD.bazel                                             #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    platform
  HDRS
    "endpoint.h"
    "instance.h"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    host_memory
  HDRS
    "host_memory.h"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
set(_native_host_memory_platform_deps "")
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  list(APPEND _native_host_memory_platform_deps libamdf::src::platform::linux::host_memory)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
  list(APPEND _native_host_memory_platform_deps libamdf::src::platform::windows::host_memory)
endif()
iree_cc_library(
  NAME
    native_host_memory
  DEPS
    amdf::headers
    ${_native_host_memory_platform_deps}
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    wait
  HDRS
    "wait.h"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
set(_native_instance_platform_deps "")
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  list(APPEND _native_instance_platform_deps libamdf::src::platform::linux::instance)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
  list(APPEND _native_instance_platform_deps libamdf::src::platform::windows::instance)
endif()
iree_cc_library(
  NAME
    native_instance
  DEPS
    amdf::headers
    ${_native_instance_platform_deps}
  PUBLIC
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
set(_native_wait_platform_deps "")
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  list(APPEND _native_wait_platform_deps libamdf::src::platform::linux::wait)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
  list(APPEND _native_wait_platform_deps libamdf::src::platform::windows::wait)
endif()
iree_cc_library(
  NAME
    native_wait
  DEPS
    amdf::headers
    ${_native_wait_platform_deps}
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
