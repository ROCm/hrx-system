################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libamdf/src/platform/linux/BUILD.bazel                                       #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD)
iree_cc_library(
  NAME
    host_cache
  HDRS
    "host_cache.h"
  SRCS
    "host_cache_x86_64.c"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND AMDF_BUILD)
iree_cc_library(
  NAME
    host_memory
  SRCS
    "host_memory.c"
  DEPS
    ::file
    ::host_cache
    amdf::headers
    libamdf::src::platform::host_memory
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND AMDF_BUILD)
iree_cc_library(
  NAME
    file
  HDRS
    "file.h"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND AMDF_BUILD)
iree_cc_library(
  NAME
    dma_buf
  HDRS
    "dma_buf.h"
  SRCS
    "dma_buf.c"
  DEPS
    ::file
    amdf::headers
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND AMDF_BUILD)
iree_cc_library(
  NAME
    wait
  SRCS
    "wait.c"
  DEPS
    ::file
    amdf::headers
    libamdf::src::platform::wait
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND AMDF_BUILD)
iree_cc_library(
  NAME
    instance
  HDRS
    "instance.h"
  SRCS
    "instance.c"
  DEPS
    ::file
    amdf::headers
    libamdf::src::allocator
    libamdf::src::platform
  LINKOPTS
    "-pthread"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND AMDF_BUILD)
iree_cc_library(
  NAME
    endpoint
  HDRS
    "endpoint.h"
  SRCS
    "endpoint.c"
  DEPS
    $<BUILD_LOCAL_INTERFACE:iree::third_party::linux_drm_uapi>
    ::file
    ::instance
    amdf::headers
    libamdf::src::allocator
    libamdf::src::pci
    libamdf::src::platform
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND AMDF_BUILD)
iree_cc_library(
  NAME
    linux
  DEPS
    ::endpoint
    ::instance
    amdf::headers
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND AMDF_BUILD)
iree_cc_test(
  NAME
    dma_buf_test
  SRCS
    "dma_buf_test.cc"
  DEPS
    ::dma_buf
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND AMDF_BUILD)
iree_cc_test(
  NAME
    endpoint_test
  SRCS
    "endpoint_test.cc"
  DEPS
    $<BUILD_LOCAL_INTERFACE:iree::third_party::linux_drm_uapi>
    ::endpoint
    ::file
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
    libamdf::src::allocator
    libamdf::src::platform
  LABELS
    "iree-run-requirement=libamdf.resource.amd_gpu"
    "iree-run-requirement=libamdf.resource.xdna"
    "runtime-resource=amd-gpu"
    "runtime-resource=amd-xdna"
    "iree-build-requirement=libamdf"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND AMDF_BUILD)
iree_cc_test(
  NAME
    endpoint_metadata_test
  SRCS
    "endpoint_metadata_test.cc"
  DEPS
    ::endpoint
    ::file
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
    libamdf::src::allocator
    libamdf::src::platform
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
