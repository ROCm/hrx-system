################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libamdf/src/gpu/umd/BUILD.bazel                                              #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    instance
  HDRS
    "instance.h"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    device
  HDRS
    "device.h"
  DEPS
    ::instance
    amdf::headers
    libamdf::src::platform
  PUBLIC
)
endif()

if(AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    endpoint_profile
  HDRS
    "endpoint_profile.h"
  DEPS
    amdf::headers
    libamdf::src::gpu::endpoint_profile
    libamdf::src::platform
  PUBLIC
)
endif()

if(AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    memory
  HDRS
    "memory.h"
  DEPS
    amdf::headers
    libamdf::src::memory_pair
    libamdf::src::memory_profile
  PUBLIC
)
endif()

if(AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    kernel_queue
  HDRS
    "kernel_queue.h"
  DEPS
    ::device
    amdf::headers
    libamdf::src::wait
  PUBLIC
)
endif()

if(AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    user_queue
  HDRS
    "user_queue.h"
  DEPS
    ::device
    amdf::headers
    libamdf::src::wait
  PUBLIC
)
endif()

if(IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA) AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
set(_umd_platform_deps "")
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  list(APPEND _umd_platform_deps libamdf::src::gpu::umd::kfd)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
  list(APPEND _umd_platform_deps libamdf::src::gpu::umd::wddm)
endif()
iree_cc_library(
  NAME
    umd
  DEPS
    ::device
    ::endpoint_profile
    ::instance
    ::kernel_queue
    ::memory
    ::user_queue
    amdf::headers
    ${_umd_platform_deps}
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
