################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libamdf/src/BUILD.bazel                                                      #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    atomics
  HDRS
    "atomics.h"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    allocator
  HDRS
    "allocator.h"
  SRCS
    "allocator.c"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_test(
  NAME
    allocator_test
  SRCS
    "allocator_test.cc"
  DEPS
    ::allocator
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    wait
  HDRS
    "wait.h"
  SRCS
    "wait.c"
  DEPS
    amdf::headers
    libamdf::src::platform::native_wait
    libamdf::src::platform::wait
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    child_tracker
  HDRS
    "child_tracker.h"
  SRCS
    "child_tracker.c"
  DEPS
    ::atomics
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    structure
  HDRS
    "structure.h"
  SRCS
    "structure.c"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    pci
  HDRS
    "pci.h"
  SRCS
    "pci.c"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    instance
  HDRS
    "instance.h"
  SRCS
    "instance.c"
  DEPS
    ::child_tracker
    ::memory_scope_state
    amdf::headers
    libamdf::src::platform
  PUBLIC
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_cc_test(
  NAME
    instance_test
  SRCS
    "instance_test.cc"
  DEPS
    ::instance
    amdf::amdf_static
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    endpoint
  HDRS
    "endpoint.h"
  SRCS
    "endpoint.c"
  DEPS
    ::allocator
    ::child_tracker
    ::instance
    ::memory_profile
    ::memory_scope_state
    ::structure
    amdf::headers
    libamdf::src::platform
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    device
  HDRS
    "device.h"
  SRCS
    "device.c"
  DEPS
    ::allocator
    ::child_tracker
    ::endpoint
    ::instance
    ::memory_profile
    amdf::headers
    libamdf::src::platform
    libamdf::src::platform::native_instance
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    memory_profile
  HDRS
    "memory_profile.h"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    memory_scope_state
  HDRS
    "memory_scope.h"
  DEPS
    ::memory_profile
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    memory_scope
  SRCS
    "memory_scope.c"
  DEPS
    ::allocator
    ::device
    ::endpoint
    ::instance
    ::memory_profile
    ::memory_scope_state
    ::structure
    amdf::headers
    libamdf::src::platform
    libamdf::src::platform::native_instance
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    memory_resource
  HDRS
    "memory_resource.h"
  SRCS
    "memory_resource.c"
  DEPS
    ::allocator
    ::child_tracker
    ::memory_pair
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    host_memory
  HDRS
    "host_memory.h"
  SRCS
    "host_memory.c"
  DEPS
    ::allocator
    ::host_mapping
    ::memory_profile
    ::memory_resource
    amdf::headers
    libamdf::src::platform::host_memory
    libamdf::src::platform::native_host_memory
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    memory
  HDRS
    "memory.h"
  SRCS
    "memory.c"
  DEPS
    ::allocator
    ::child_tracker
    ::device
    ::host_mapping
    ::host_memory
    ::instance
    ::memory_pair
    ::memory_profile
    ::memory_resource
    ::memory_scope
    ::memory_scope_state
    ::structure
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    memory_pair
  HDRS
    "memory_pair.h"
  DEPS
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
set(_memory_test_platform_deps "")
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  list(APPEND _memory_test_platform_deps libamdf::src::platform::linux)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
  list(APPEND _memory_test_platform_deps libamdf::src::platform::windows)
endif()
iree_cc_test(
  NAME
    memory_test
  SRCS
    "memory_construction_test.cc"
    "memory_test.cc"
    "memory_test_fixture.cc"
    "memory_test_fixture.h"
  DEPS
    ::allocator
    ::child_tracker
    ::device
    ::host_mapping
    ::instance
    ::memory
    ::memory_scope
    ::memory_scope_state
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
    ${_memory_test_platform_deps}
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    host_mapping
  HDRS
    "host_mapping.h"
  SRCS
    "host_mapping.c"
  DEPS
    ::allocator
    ::memory_resource
    ::structure
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
set(_host_mapping_test_platform_deps "")
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  list(APPEND _host_mapping_test_platform_deps libamdf::src::platform::linux)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
  list(APPEND _host_mapping_test_platform_deps libamdf::src::platform::windows)
endif()
iree_cc_test(
  NAME
    host_mapping_test
  SRCS
    "host_mapping_test.cc"
  DEPS
    ::host_mapping
    ::memory
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
    ${_host_mapping_test_platform_deps}
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    kernel_queue
  HDRS
    "kernel_queue.h"
  SRCS
    "kernel_queue.c"
  DEPS
    ::allocator
    ::device
    ::structure
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    user_queue
  HDRS
    "user_queue.h"
  SRCS
    "user_queue.c"
  DEPS
    ::allocator
    ::child_tracker
    ::device
    ::structure
    amdf::headers
  PUBLIC
)
endif()

if(AMDF_BUILD)
set(_user_queue_test_platform_deps "")
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  list(APPEND _user_queue_test_platform_deps libamdf::src::platform::linux)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
  list(APPEND _user_queue_test_platform_deps libamdf::src::platform::windows)
endif()
iree_cc_test(
  NAME
    user_queue_test
  SRCS
    "user_queue_test.cc"
  DEPS
    ::allocator
    ::child_tracker
    ::device
    ::user_queue
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
    ${_user_queue_test_platform_deps}
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
set(_core_platform_deps "")
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  list(APPEND _core_platform_deps libamdf::src::platform::linux)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
  list(APPEND _core_platform_deps libamdf::src::platform::windows)
endif()
iree_cc_library(
  NAME
    core
  DEPS
    ::device
    ::endpoint
    ::host_mapping
    ::instance
    ::kernel_queue
    ::memory
    ::pci
    ::structure
    ::user_queue
    amdf::headers
    ${_core_platform_deps}
  PUBLIC
)
endif()

if(AMDF_BUILD)
iree_cc_test(
  NAME
    wait_test
  SRCS
    "wait_test.cc"
  DEPS
    ::wait
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
    libamdf::src::platform::wait
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD)
iree_cc_test(
  NAME
    atomics_test
  SRCS
    "atomics_test.cc"
  DEPS
    ::atomics
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD)
iree_cc_test(
  NAME
    pci_test
  SRCS
    "pci_test.cc"
  DEPS
    ::pci
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
