################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tooling/compile/configured/BUILD.bazel                         #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

set(_emitter_platform_copts "")
if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
  list(APPEND _emitter_platform_copts "-DLOOM_CONFIG_EMITTER_HAVE_LLVMIR=1")
endif()
if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR AND LOOM_TARGET_ARCH_AMDGPU)
  list(APPEND _emitter_platform_copts "-DLOOM_CONFIG_EMITTER_HAVE_LLVMIR_AMDGPU=1")
endif()
if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR AND LOOM_TARGET_ARCH_X86)
  list(APPEND _emitter_platform_copts "-DLOOM_CONFIG_EMITTER_HAVE_LLVMIR_X86=1")
endif()
set(_emitter_platform_deps "")
if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
  list(APPEND _emitter_platform_deps loom::target::emit::llvmir::artifact_emitter)
endif()
if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR AND LOOM_TARGET_ARCH_AMDGPU)
  list(APPEND _emitter_platform_deps loom::target::emit::llvmir::amdgpu::target_env)
endif()
if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR AND LOOM_TARGET_ARCH_X86)
  list(APPEND _emitter_platform_deps loom::target::emit::llvmir::x86::target_env)
endif()
loom_cc_library(
  NAME
    emitter
  COPTS
    ${_emitter_platform_copts}
  HDRS
    "emitter.h"
  SRCS
    "emitter.c"
  DEPS
    iree::base
    loom::target::provider
    ${_emitter_platform_deps}
  PUBLIC
)

set(_registry_platform_copts "")
if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
  list(APPEND _registry_platform_copts "-DLOOM_CONFIG_PRODUCT_HAVE_AMDGPU_HSACO=1")
endif()
if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
  list(APPEND _registry_platform_copts "-DLOOM_CONFIG_PRODUCT_HAVE_SPIRV_BINARY=1")
endif()
if(LOOM_TARGET_ARCH_XDNA AND LOOM_EMIT_XDNA)
  list(APPEND _registry_platform_copts "-DLOOM_CONFIG_PRODUCT_HAVE_XDNA=1")
endif()
set(_registry_platform_deps "")
if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
  list(APPEND _registry_platform_deps loom::tooling::target::amdgpu::product_provider)
endif()
if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
  list(APPEND _registry_platform_deps loom::tooling::target::spirv::product_provider)
endif()
if(LOOM_TARGET_ARCH_XDNA AND LOOM_EMIT_XDNA)
  list(APPEND _registry_platform_deps loom::tooling::target::amd::xdna::product_provider)
endif()
loom_cc_library(
  NAME
    registry
  COPTS
    ${_registry_platform_copts}
  HDRS
    "registry.h"
  SRCS
    "registry.c"
  DEPS
    iree::base
    loom::product::kernel
    loom::product::registry
    loom::target::arch::cmd::product
    loom::tooling::target::cmd::product_provider
    ${_registry_platform_deps}
  PUBLIC
)

set(_registry_test_platform_copts "")
if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
  list(APPEND _registry_test_platform_copts "-DLOOM_CONFIG_PRODUCT_HAVE_AMDGPU_HSACO=1")
endif()
if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
  list(APPEND _registry_test_platform_copts "-DLOOM_CONFIG_PRODUCT_HAVE_SPIRV_BINARY=1")
endif()
if(LOOM_TARGET_ARCH_XDNA AND LOOM_EMIT_XDNA)
  list(APPEND _registry_test_platform_copts "-DLOOM_CONFIG_PRODUCT_HAVE_XDNA=1")
endif()
set(_registry_test_platform_deps "")
if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
  list(APPEND _registry_test_platform_deps loom::tooling::target::amdgpu::product_provider)
endif()
if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
  list(APPEND _registry_test_platform_deps loom::tooling::target::spirv::product_provider)
endif()
if(LOOM_TARGET_ARCH_XDNA AND LOOM_EMIT_XDNA)
  list(APPEND _registry_test_platform_deps loom::tooling::target::amd::xdna::product_provider)
endif()
loom_cc_test(
  NAME
    registry_test
  SRCS
    "registry_test.cc"
  COPTS
    ${_registry_test_platform_copts}
  DEPS
    ::registry
    iree::testing::gtest
    iree::testing::gtest_main
    loom::product::kernel
    loom::target::arch::cmd::product
    ${_registry_test_platform_deps}
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
