# Copyright 2026 The IREE Authors
#
# Licensed under the Apache License v2.0 with LLVM Exceptions.
# See https://llvm.org/LICENSE.txt for license information.
# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

load(
    "//loom/build_tools/bazel:build_defs.bzl",
    "loom_cc_library",
    "loom_cc_test",
)

package(
    default_visibility = ["//visibility:private"],
    features = ["layering_check"],
    licenses = ["notice"],  # Apache 2.0
)

loom_cc_library(
    name = "execution_provider_verify",
    testonly = True,
    srcs = ["execution_provider_verify.c"],
    hdrs = ["execution_provider_verify.h"],
    deps = [
        "//loom/src/loom/tooling/execution:execution_provider",
        "//runtime/src/iree/base",
    ],
)

loom_cc_test(
    name = "execution_provider_verify_test",
    srcs = ["execution_provider_verify_test.cc"],
    deps = [
        ":execution_provider_verify",
        "//loom/src/loom/target:low_descriptor_registry_core_test",
        "//loom/src/loom/tooling/execution:execution_provider",
        "//runtime/src/iree/testing:gtest",
        "//runtime/src/iree/testing:gtest_main",
    ],
)
