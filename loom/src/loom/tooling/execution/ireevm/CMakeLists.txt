################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tooling/execution/ireevm/BUILD.bazel                           #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_IREE_VM AND LOOM_EMIT_IREE_VM AND LOOM_EXECUTE_IREE_VM)
loom_cc_library(
  NAME
    candidate
  HDRS
    "candidate.h"
  SRCS
    "candidate.c"
  DEPS
    iree::base
    loom::target::emit::ireevm::archive_emitter
    loom::target::emit::ireevm::module_archive
    loom::tooling::execution::compile_options
    loom::tooling::execution::session
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM AND LOOM_EMIT_IREE_VM AND LOOM_EXECUTE_IREE_VM)
loom_cc_library(
  NAME
    invocation
  HDRS
    "invocation.h"
  SRCS
    "invocation.c"
  DEPS
    iree::base
    iree::hal
    iree::io::stream
    iree::tooling::comparison
    iree::tooling::context_util
    iree::tooling::function_io
    iree::tooling::function_util
    iree::vm
    iree::vm::bytecode::module
    loom::target::emit::ireevm::module_archive
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM AND LOOM_EMIT_IREE_VM AND LOOM_EXECUTE_IREE_VM)
loom_cc_library(
  NAME
    backend
  HDRS
    "backend.h"
  SRCS
    "backend.c"
  DEPS
    ::candidate
    ::invocation
    iree::base
    loom::tooling::execution::compile_report_capture
    loom::tooling::execution::execution_backend
    loom::tooling::execution::one_shot
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM AND LOOM_EMIT_IREE_VM AND LOOM_EXECUTE_IREE_VM)
loom_cc_library(
  NAME
    provider
  HDRS
    "provider.h"
  SRCS
    "provider.c"
  DEPS
    ::backend
    iree::base
    loom::target::arch::ireevm::provider
    loom::tooling::execution::execution_provider
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM AND LOOM_EMIT_IREE_VM AND LOOM_EXECUTE_IREE_VM)
loom_cc_test(
  NAME
    candidate_test
  SRCS
    "candidate_test.cc"
  DEPS
    ::candidate
    ::invocation
    iree::base
    iree::base::internal::flatcc::parsing
    iree::schemas::bytecode_module_def_c_fbs
    iree::testing::gtest
    iree::testing::gtest_main
    iree::vm
    iree::vm::bytecode::archive
    iree::vm::bytecode::module
    iree::vm::impl
    loom::ops::op_registry
    loom::target::arch::ireevm::descriptors
    loom::target::arch::ireevm::ops
    loom::target::arch::ireevm::provider
    loom::target::emit::ireevm::function_bytecode
    loom::target::provider
    loom::tooling::compile::pipeline
  LABELS
    "iree-build-requirement=loom.target.arch.iree_vm"
    "iree-build-requirement=loom.emit.iree_vm"
    "iree-build-requirement=loom.execute.iree_vm"
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM AND LOOM_EMIT_IREE_VM AND LOOM_EXECUTE_IREE_VM)
loom_cc_test(
  NAME
    invocation_test
  SRCS
    "invocation_test.cc"
  DEPS
    ::candidate
    ::invocation
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
    loom::ops::op_registry
    loom::target::arch::ireevm::descriptors::low_registry
    loom::target::arch::ireevm::ops
    loom::tooling::execution::session
  LABELS
    "iree-build-requirement=loom.target.arch.iree_vm"
    "iree-build-requirement=loom.emit.iree_vm"
    "iree-build-requirement=loom.execute.iree_vm"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
