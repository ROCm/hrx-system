################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tooling/target/vm/BUILD.bazel                                  #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_VM)
loom_cc_library(
  NAME
    testbench
  HDRS
    "testbench.h"
  SRCS
    "testbench.c"
  DEPS
    iree::base
    iree::hal
    iree::vm
    iree::vm::bytecode::module
    loom::error::error_defs
    loom::error::source
    loom::link::linker
    loom::ops::func
    loom::ops::op_defs
    loom::target::arch::vm::module
    loom::target::arch::vm::provider
    loom::target::entry_selection
    loom::target::provider
    loom::tooling::compile::pipeline
    loom::tooling::config
    loom::tooling::testbench::invocation
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_VM)
loom_generated_file(
  NAME
    imports_binary
  GENERATOR
    loom::tools::loom-compile
  OUTPUT
    "imports.vm"
  OUTPUT_FLAG
    "--output="
  ARGS
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/vm/test/imports.loom"
    "--target=vm:core"
  INPUTS
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/vm/test/imports.loom"
  TESTONLY
)
endif()

if(LOOM_TARGET_ARCH_VM)
iree_c_embed_data(
  NAME
    imports_bytecode
  SRCS
    "${CMAKE_CURRENT_BINARY_DIR}/imports.vm"
  C_FILE_OUTPUT
    "imports_bytecode.c"
  H_FILE_OUTPUT
    "imports_bytecode.h"
  IDENTIFIER
    "loom_vm_imports_bytecode"
  TESTONLY
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_VM)
loom_cc_test(
  NAME
    imports_test
  SRCS
    "imports_test.cc"
  DEPS
    ::imports_bytecode
    iree::base
    iree::base::internal::arena
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
    iree::vm
    iree::vm::bytecode::module
    loom::format::bytecode::reader
    loom::format::bytecode::writer
    loom::format::location
    loom::ir
    loom::link::linker
    loom::ops::func
    loom::ops::func::location_capture
    loom::ops::func::location_capture_test_data
    loom::ops::op_defs
    loom::ops::op_registry
    loom::target::arch::vm::module
    loom::target::arch::vm::provider
    loom::target::provider
    loom::tooling::compile::pipeline
    loom::tooling::input
    loom::transforms::cleanup::configured
  LABELS
    "iree-build-requirement=loom.target.arch.vm"
)
endif()

if(LOOM_TARGET_ARCH_VM)
loom_generated_file(
  NAME
    native_references_binary
  GENERATOR
    loom::tools::loom-compile
  OUTPUT
    "native_references.vm"
  OUTPUT_FLAG
    "--output="
  ARGS
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/vm/test/native_references.loom"
    "--target=vm:core"
  INPUTS
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/vm/test/native_references.loom"
  TESTONLY
)
endif()

if(LOOM_TARGET_ARCH_VM)
iree_c_embed_data(
  NAME
    native_references_bytecode
  SRCS
    "${CMAKE_CURRENT_BINARY_DIR}/native_references.vm"
  C_FILE_OUTPUT
    "native_references_bytecode.c"
  H_FILE_OUTPUT
    "native_references_bytecode.h"
  IDENTIFIER
    "loom_vm_native_references_bytecode"
  TESTONLY
  PUBLIC
)
endif()

iree_c_embed_data(
  NAME
    emission_test_data
  SRCS
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/vm/test/native_references.loom"
  C_FILE_OUTPUT
    "emission_test_data.c"
  H_FILE_OUTPUT
    "emission_test_data.h"
  IDENTIFIER
    "loom_vm_emission_test_data"
  TESTONLY
  PUBLIC
)

if(LOOM_TARGET_ARCH_VM)
loom_cc_test(
  NAME
    emission_test
  SRCS
    "emission_test.cc"
  DEPS
    ::emission_test_data
    ::native_references_bytecode
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::ir
    loom::ops::func
    loom::ops::op_registry
    loom::target::arch::vm::module
    loom::target::arch::vm::provider
    loom::target::provider
    loom::tooling::compile::pipeline
    loom::tooling::input
    loom::transforms::cleanup::configured
  LABELS
    "iree-build-requirement=loom.target.arch.vm"
)
endif()

if(LOOM_TARGET_ARCH_VM)
loom_cc_test(
  NAME
    native_references_test
  SRCS
    "native_references_test.cc"
  DEPS
    ::native_references_bytecode
    iree::base
    iree::hal
    iree::module::hal::types
    iree::testing::gtest
    iree::testing::gtest_main
    iree::vm
    iree::vm::bytecode::module
    loom::format::location
  LABELS
    "iree-build-requirement=loom.target.arch.vm"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
