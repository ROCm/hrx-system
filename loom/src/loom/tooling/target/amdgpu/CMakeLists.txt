################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tooling/target/amdgpu/BUILD.bazel                              #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    artifact_provider
  HDRS
    "artifact_provider.h"
  SRCS
    "artifact_provider.c"
  DEPS
    iree::base
    iree::hal
    iree::hal::executable::amdgpu::executable_target
    iree::hal::executable::amdgpu::target_id
    loom::ir
    loom::ops::op_defs
    loom::target::arch::amdgpu::ops
    loom::target::arch::amdgpu::profile
    loom::target::arch::amdgpu::records::target_records
    loom::target::arch::amdgpu::runtime_requirements
    loom::target::arch::amdgpu::target_id
    loom::target::arch::amdgpu::target_info
    loom::target::emit::native::amdgpu::hal_kernel_library
    loom::target::emit::native::amdgpu::runtime_globals
    loom::target::reporting::report
    loom::target::types
    loom::tooling::execution::compile_options
    loom::tooling::execution::hal::artifact
    loom::tooling::execution::hal::runtime
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    testbench_requirements
  HDRS
    "testbench_requirements.h"
  SRCS
    "testbench_requirements.c"
  DEPS
    iree::base
    loom::target::arch::amdgpu::profile
    loom::target::arch::amdgpu::target_info_defs
    loom::tooling::execution::hal::testbench_actual
    loom::tooling::testbench::requirements
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    artifact_provider_test
  SRCS
    "artifact_provider_test.cc"
  DEPS
    ::artifact_provider
    iree::base
    iree::base::internal::arena
    iree::hal
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::text_asm
    loom::format::text::parser
    loom::ir
    loom::ops::op_registry
    loom::sanitizer::options
    loom::target::arch::amdgpu::descriptors::low_registry
    loom::target::arch::amdgpu::ops
    loom::target::arch::amdgpu::profile
    loom::target::arch::amdgpu::records::target_records
    loom::target::arch::amdgpu::target_info
    loom::target::emit::native::amdgpu::runtime_globals
    loom::target::reporting::report
    loom::testing::module_ptr
    loom::tooling::execution::hal::runtime
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
iree_execution_test_suite(
  NAME
    loom_compile_execution_test
  MANIFESTS
    "test/loom-compile-amdgpu.test.json"
  TOOLS
    "loom-compile=loom::tools::loom-compile"
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/amdgpu_bad_return.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/amdgpu_unused_kernargs.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/gfx1250_configured_fragment_bank.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/selected_root_amdgpu.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/target_family_template_provider_amdgpu.loom"
  LABELS
    "hostonly"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
iree_execution_test_suite(
  NAME
    loom_opt_execution_test
  MANIFESTS
    "test/loom-opt-amdgpu.test.json"
  TOOLS
    "loom-opt=loom::tools::loom-opt"
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/amdgpu_bad_return.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/loom_opt_f32_arith.loom"
  LABELS
    "hostonly"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_AMDGPU)
iree_execution_test_suite(
  NAME
    iree_test_loom_execution_test
  MANIFESTS
    "test/iree-test-loom_amdgpu.test.json"
  TOOLS
    "iree-test-loom=loom::tools::iree-test-loom"
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/amdgpu_compile_rejection.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/amdgpu_hal_actual.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/amdgpu_unused_kernargs.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/generated_tensor_cases.loom"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "requires-gpu-amd"
    "runtime-resource=amd-gpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_AMDGPU)
iree_execution_test_suite(
  NAME
    iree_test_loom_asan_execution_test
  MANIFESTS
    "test/iree-test-loom_amdgpu_asan.test.json"
  TOOLS
    "iree-test-loom=loom::tools::iree-test-loom"
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/sanitizer/amdgpu_asan_actual.loom"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "requires-gpu-amd"
    "runtime-resource=amd-gpu"
    "notsan"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_AMDGPU)
iree_execution_test_suite(
  NAME
    iree_test_loom_tsan_execution_test
  MANIFESTS
    "test/iree-test-loom_amdgpu_tsan.test.json"
  TOOLS
    "iree-test-loom=loom::tools::iree-test-loom"
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/sanitizer/amdgpu_tsan_actual.loom"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "requires-gpu-amd"
    "runtime-resource=amd-gpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_AMDGPU)
iree_execution_test_suite(
  NAME
    iree_benchmark_loom_execution_test
  MANIFESTS
    "test/iree-benchmark-loom_amdgpu.test.json"
  TOOLS
    "iree-benchmark-loom=loom::tools::iree-benchmark-loom"
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amdgpu/test/amdgpu_hal_actual.loom"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "requires-gpu-amd"
    "runtime-resource=amd-gpu"
    "manual"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
