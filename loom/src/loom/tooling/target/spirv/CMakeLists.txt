################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tooling/target/spirv/BUILD.bazel                               #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_BUILD AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    artifact_provider
  HDRS
    "artifact_provider.h"
  SRCS
    "artifact_provider.c"
  DEPS
    iree::base
    loom::target::arch::spirv::descriptors::low_registry
    loom::target::arch::spirv::profile
    loom::target::emit::spirv::module_builder
    loom::target::emit::spirv::module_emitter
    loom::target::entry_selection
    loom::target::function_contract
    loom::target::low_descriptor_registry
    loom::target::reporting::artifact_manifest_collect
    loom::target::reporting::report
    loom::target::types
    loom::tooling::compile::artifact
  PUBLIC
)
endif()

if(LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    device_provider
  HDRS
    "device_provider.h"
  SRCS
    "device_provider.c"
  DEPS
    ::artifact_provider
    ::vulkan_profile
    iree::base
    loom::target::arch::spirv::facts
    loom::target::arch::spirv::profile
    loom::tooling::execution::hal::device_provider
    loom::tooling::execution::hal::runtime
  PUBLIC
)
endif()

if(LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    execution_provider
  HDRS
    "execution_provider.h"
  SRCS
    "execution_provider.c"
  DEPS
    ::device_provider
    iree::base
    loom::target::arch::spirv::provider
    loom::tooling::execution::execution_provider
    loom::tooling::execution::hal::execution_backend
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    testbench_requirements
  HDRS
    "testbench_requirements.h"
  SRCS
    "testbench_requirements.c"
  DEPS
    ::vulkan_profile
    iree::base
    iree::hal::drivers::vulkan::device_spec
    loom::tooling::execution::hal::testbench_actual
    loom::tooling::testbench::requirements
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    vulkan_profile
  HDRS
    "vulkan_profile.h"
  SRCS
    "vulkan_profile.c"
  DEPS
    iree::base
    iree::hal
    iree::hal::drivers::vulkan
    iree::hal::drivers::vulkan::api
    iree::hal::drivers::vulkan::device_spec
    loom::target::arch::spirv::cooperative_properties
    loom::target::arch::spirv::features
    loom::target::arch::spirv::profile
    loom::target::arch::spirv::records::target_records
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_BUILD AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    artifact_provider_test
  SRCS
    "artifact_provider_test.cc"
  DEPS
    ::artifact_provider
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::text_asm
    loom::format::text::parser
    loom::ir
    loom::ops::op_registry
    loom::target::arch::spirv::descriptors::low_registry
    loom::target::arch::spirv::ops
    loom::testing::byte_sequence
    loom::testing::module_ptr
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    device_provider_test
  SRCS
    "device_provider_test.cc"
  DEPS
    ::device_provider
    ::vulkan_profile
    iree::base
    iree::hal
    iree::hal::drivers::vulkan::device_spec
    iree::hal::utils::device_spec_builder
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::arch::spirv::profile
    loom::tooling::execution::hal::runtime
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    module_emitter_test
  SRCS
    "module_emitter_test.cc"
  DEPS
    ::vulkan_profile
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::analysis::symbol_facts
    loom::codegen::low::text_asm
    loom::format::text::parser
    loom::ir
    loom::ops::func_symbol_facts
    loom::ops::op_defs
    loom::ops::op_registry
    loom::ops::target
    loom::target::arch::spirv::descriptors::low_registry
    loom::target::arch::spirv::features
    loom::target::arch::spirv::ops
    loom::target::arch::spirv::profile
    loom::target::emit::spirv::module_emitter
    loom::target::facts_builder
    loom::target::function_contract
    loom::target::function_version
    loom::target::profile
    loom::target::types
    loom::testing::module_ptr
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    vulkan_profile_test
  SRCS
    "vulkan_profile_test.cc"
  DEPS
    ::vulkan_profile
    iree::base
    iree::hal
    iree::hal::drivers::vulkan::api
    iree::hal::drivers::vulkan::device_spec
    iree::hal::utils::device_spec_builder
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::arch::spirv::cooperative_properties
    loom::target::arch::spirv::features
    loom::target::arch::spirv::profile
    loom::target::types
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_test_module(
  NAME
    vulkan_explicit_target_tests_module
  SRCS
    "test/spirv_vulkan_hal_actual.loom"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
loom_execution_test(
  NAME
    vulkan_explicit_target_tests_execute_spirv_vulkan_explicit_target_hardware_test
  MODULE
    "::vulkan_explicit_target_tests_module"
  RUNNER_ARGS
    "--device=vulkan"
    "--vulkan_validation_layers=false"
    "--target=spirv:vulkan1.3+bda"
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
    "iree-build-requirement=loom.execute.iree_hal"
    "iree-build-requirement=runtime.hal.vulkan"
    "iree-run-requirement=vulkan.resource.device"
    "runtime-resource=vulkan-device"
    "loom-execution-profile=spirv_vulkan_explicit_target_hardware"
    "loom-target-family=spirv"
    "loom-target-class=gpu"
    "loom-executor=hardware"
  RESOURCE_GROUP
    "gpu-device"
)
endif()

loom_check_compile_tests(
  NAME
    "vulkan_explicit_target_tests"
  SRC
    "::vulkan_explicit_target_tests_module"
  TARGETS
    loom::target::spirv::vulkan1.3+bda
)
endif()

if(LOOM_IMPORT_CXX AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
loom_execution_test(
  NAME
    cxx_packed_byte_arithmetic_test_execute_spirv_vulkan_hardware_test
  MODULE
    "loom::import::cxx::test::packed_byte_arithmetic_module"
  RUNNER_ARGS
    "--device=vulkan"
    "--vulkan_validation_layers=false"
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
    "iree-build-requirement=loom.execute.iree_hal"
    "iree-build-requirement=runtime.hal.vulkan"
    "iree-run-requirement=vulkan.resource.device"
    "runtime-resource=vulkan-device"
    "loom-execution-profile=spirv_vulkan_hardware"
    "loom-target-family=spirv"
    "loom-target-class=gpu"
    "loom-executor=hardware"
  RESOURCE_GROUP
    "gpu-device"
)
endif()
endif()

if(LOOM_IMPORT_CXX AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
loom_execution_test(
  NAME
    cxx_packed_float8_conversion_test_execute_spirv_vulkan_hardware_test
  MODULE
    "loom::import::cxx::test::packed_float8_conversion_module"
  RUNNER_ARGS
    "--device=vulkan"
    "--vulkan_validation_layers=false"
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
    "iree-build-requirement=loom.execute.iree_hal"
    "iree-build-requirement=runtime.hal.vulkan"
    "iree-run-requirement=vulkan.resource.device"
    "runtime-resource=vulkan-device"
    "loom-execution-profile=spirv_vulkan_hardware"
    "loom-target-family=spirv"
    "loom-target-class=gpu"
    "loom-executor=hardware"
  RESOURCE_GROUP
    "gpu-device"
)
endif()
endif()

if(LOOM_IMPORT_CXX AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
loom_execution_test(
  NAME
    cxx_vector_lookup_test_execute_spirv_vulkan_hardware_test
  MODULE
    "loom::import::cxx::test::vector_lookup_module"
  RUNNER_ARGS
    "--device=vulkan"
    "--vulkan_validation_layers=false"
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
    "iree-build-requirement=loom.execute.iree_hal"
    "iree-build-requirement=runtime.hal.vulkan"
    "iree-run-requirement=vulkan.resource.device"
    "runtime-resource=vulkan-device"
    "loom-execution-profile=spirv_vulkan_hardware"
    "loom-target-family=spirv"
    "loom-target-class=gpu"
    "loom-executor=hardware"
  RESOURCE_GROUP
    "gpu-device"
)
endif()
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_test_module(
  NAME
    buffer_selection_test_module
  SRCS
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/source_low/buffer_selection.loom"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
loom_execution_test(
  NAME
    buffer_selection_test_execute_spirv_vulkan_hardware_test
  MODULE
    "::buffer_selection_test_module"
  RUNNER_ARGS
    "--device=vulkan"
    "--vulkan_validation_layers=false"
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
    "iree-build-requirement=loom.execute.iree_hal"
    "iree-build-requirement=runtime.hal.vulkan"
    "iree-run-requirement=vulkan.resource.device"
    "runtime-resource=vulkan-device"
    "loom-execution-profile=spirv_vulkan_hardware"
    "loom-target-family=spirv"
    "loom-target-class=gpu"
    "loom-executor=hardware"
  RESOURCE_GROUP
    "gpu-device"
)
endif()

loom_check_compile_tests(
  NAME
    "buffer_selection_test"
  SRC
    "::buffer_selection_test_module"
  TARGETS
    loom::target::spirv::vulkan1.3+bda
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_test_module(
  NAME
    vulkan_tests_module
  SRCS
    "test/multiple_entries.loom"
    "test/spirv_vulkan_atomic_hal_actual.loom"
    "test/spirv_vulkan_coop_hal_actual.loom"
    "test/spirv_vulkan_coop_u8_hal_actual.loom"
    "test/spirv_vulkan_hal_actual.loom"
    "test/spirv_vulkan_integer_hal_actual.loom"
    "test/spirv_vulkan_quantized_hal_actual.loom"
    "test/spirv_vulkan_vector_float.loom"
    "test/spirv_vulkan_workgroup_offsets.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/checked_benchmarks/moe_routed_combine_loop_f32.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/conformance/atomic_add_f32_subnormals.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/conformance/global_atomic_add_f32.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/conformance/loop_rotating_values.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/conformance/loop_rotating_views.loom"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
loom_execution_test(
  NAME
    vulkan_tests_execute_spirv_vulkan_hardware_test
  MODULE
    "::vulkan_tests_module"
  RUNNER_ARGS
    "--device=vulkan"
    "--vulkan_validation_layers=false"
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
    "iree-build-requirement=loom.execute.iree_hal"
    "iree-build-requirement=runtime.hal.vulkan"
    "iree-run-requirement=vulkan.resource.device"
    "runtime-resource=vulkan-device"
    "loom-execution-profile=spirv_vulkan_hardware"
    "loom-target-family=spirv"
    "loom-target-class=gpu"
    "loom-executor=hardware"
  RESOURCE_GROUP
    "gpu-device"
)
endif()
endif()

if(LOOM_BUILD AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
iree_native_test(
  NAME
    "aliased_entry_test"
  ARGS
    "{{${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/spirv/test/multiple_entries.loom}}"
    "--device=vulkan"
    "--vulkan_validation_layers=false"
    "--function=@combine"
    "--kernel-input-buffer=1xi32=6"
    "--kernel-input-buffer=1xi32=10"
    "--kernel-input-buffer=1xi32=0"
    "--kernel-input-value=i32=4"
    "--kernel-input-value=i32=5"
    "--expected-kernel-buffer=1xi32=6"
    "--expected-kernel-buffer=1xi32=10"
    "--expected-kernel-buffer=1xi32=25"
  SRC
    loom::tools::iree-run-loom
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/spirv/test/multiple_entries.loom"
  LABELS
    "iree-run-requirement=vulkan.resource.device"
    "runtime-resource=vulkan-device"
  RESOURCE_GROUP
    gpu-device
)
endif()

if(LOOM_BUILD AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_generated_file(
  NAME
    multiple_entries_binary
  GENERATOR
    loom::tools::loom-compile::loom-compile
  OUTPUT
    "multiple_entries.spv"
  OUTPUT_FLAG
    "--output="
  ARGS
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/spirv/test/multiple_entries.loom"
    "--product=kernel"
    "--target=spirv:vulkan1.3+bda"
  INPUTS
    "test/multiple_entries.loom"
  TESTONLY
)
endif()

if(LOOM_BUILD AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
iree_c_embed_data(
  NAME
    multiple_entries_spv
  SRCS
    "${CMAKE_CURRENT_BINARY_DIR}/multiple_entries.spv"
  C_FILE_OUTPUT
    "multiple_entries_spv.c"
  H_FILE_OUTPUT
    "multiple_entries_spv.h"
  IDENTIFIER
    "loom_spirv_multiple_entries"
  TESTONLY
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    artifact_execution_test
  SRCS
    "${PROJECT_SOURCE_DIR}/runtime/src/iree/hal/cts/util/test_main.cc"
    "artifact_execution_test.cc"
  DEPS
    ::multiple_entries_spv
    iree::base::tooling::flags
    iree::hal
    iree::hal::cts::util::registry
    iree::hal::cts::util::test_base
    iree::hal::drivers::vulkan::cts::backends
    iree::testing::gtest
  ARGS
    "--vulkan_validation_layers=false"
  LABELS
    "iree-run-requirement=vulkan.resource.device"
    "runtime-resource=vulkan-device"
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
  RESOURCE_GROUP
    gpu-device
)
endif()

if(LOOM_BUILD AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_generated_file(
  NAME
    float8_memory_binary
  GENERATOR
    loom::tools::loom-compile::loom-compile
  OUTPUT
    "float8_memory.spv"
  OUTPUT_FLAG
    "--output="
  ARGS
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/source_low/memory_float8_conversion.loom"
    "--product=kernel"
    "--target=spirv:vulkan1.3+bda+extended-types"
  INPUTS
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/source_low/memory_float8_conversion.loom"
  TESTONLY
)
endif()

if(LOOM_BUILD AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
iree_c_embed_data(
  NAME
    float8_memory_spv
  SRCS
    "${CMAKE_CURRENT_BINARY_DIR}/float8_memory.spv"
  C_FILE_OUTPUT
    "float8_memory_spv.c"
  H_FILE_OUTPUT
    "float8_memory_spv.h"
  IDENTIFIER
    "loom_spirv_float8_memory"
  TESTONLY
  PUBLIC
)
endif()

if(LOOM_BUILD AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    float8_memory_test
  SRCS
    "${PROJECT_SOURCE_DIR}/runtime/src/iree/hal/cts/util/test_main.cc"
    "float8_memory_test.cc"
  DEPS
    ::float8_memory_spv
    iree::base::tooling::flags
    iree::hal
    iree::hal::cts::util::registry
    iree::hal::cts::util::test_base
    iree::hal::drivers::vulkan::cts::backends
    iree::testing::gtest
  ARGS
    "--vulkan_validation_layers=false"
  LABELS
    "iree-run-requirement=vulkan.resource.device"
    "runtime-resource=vulkan-device"
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
  RESOURCE_GROUP
    gpu-device
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
