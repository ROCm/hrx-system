################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tooling/target/spirv/BUILD.bazel                               #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    artifact_provider
  HDRS
    "artifact_provider.h"
  SRCS
    "artifact_provider.c"
  DEPS
    iree::base
    loom::target::arch::spirv::descriptors::low_registry
    loom::target::arch::spirv::low_verify
    loom::target::emit::spirv::module_builder
    loom::target::emit::spirv::module_emitter
    loom::target::entry_selection
    loom::target::function_contract
    loom::target::low_descriptor_registry
    loom::target::reporting::artifact_manifest_collect
    loom::target::reporting::report
    loom::target::types
    loom::tooling::compile::artifact
  PUBLIC
)
endif()

if(LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    device_provider
  HDRS
    "device_provider.h"
  SRCS
    "device_provider.c"
  DEPS
    ::artifact_provider
    ::vulkan_profile
    iree::base
    loom::target::arch::spirv::facts
    loom::tooling::execution::hal::device_provider
    loom::tooling::execution::hal::runtime
  PUBLIC
)
endif()

if(LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    execution_provider
  HDRS
    "execution_provider.h"
  SRCS
    "execution_provider.c"
  DEPS
    ::device_provider
    iree::base
    loom::target::arch::spirv::provider
    loom::tooling::execution::execution_provider
    loom::tooling::execution::hal::execution_backend
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    testbench_requirements
  HDRS
    "testbench_requirements.h"
  SRCS
    "testbench_requirements.c"
  DEPS
    ::vulkan_profile
    iree::base
    iree::hal::drivers::vulkan::device_spec
    loom::tooling::execution::hal::testbench_actual
    loom::tooling::testbench::requirements
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    vulkan_profile
  HDRS
    "vulkan_profile.h"
  SRCS
    "vulkan_profile.c"
  DEPS
    iree::base
    iree::hal
    iree::hal::drivers::vulkan
    iree::hal::drivers::vulkan::api
    iree::hal::drivers::vulkan::device_spec
    loom::target::arch::spirv::cooperative_properties
    loom::target::arch::spirv::features
    loom::target::arch::spirv::profile
    loom::target::arch::spirv::records::target_records
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    artifact_provider_test
  SRCS
    "artifact_provider_test.cc"
  DEPS
    ::artifact_provider
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::text_asm
    loom::format::text::parser
    loom::ir
    loom::ops::op_registry
    loom::target::arch::spirv::descriptors::low_registry
    loom::target::arch::spirv::ops
    loom::testing::byte_sequence
    loom::testing::module_ptr
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    device_provider_test
  SRCS
    "device_provider_test.cc"
  DEPS
    ::device_provider
    ::vulkan_profile
    iree::base
    iree::hal
    iree::hal::drivers::vulkan::device_spec
    iree::hal::utils::device_spec_builder
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::arch::spirv::profile
    loom::tooling::execution::hal::runtime
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    module_emitter_test
  SRCS
    "module_emitter_test.cc"
  DEPS
    ::vulkan_profile
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::analysis::symbol_facts
    loom::codegen::low::text_asm
    loom::format::text::parser
    loom::ir
    loom::ops::func_symbol_facts
    loom::ops::op_defs
    loom::ops::op_registry
    loom::ops::target
    loom::target::arch::spirv::descriptors::low_registry
    loom::target::arch::spirv::features
    loom::target::arch::spirv::ops
    loom::target::arch::spirv::profile
    loom::target::emit::spirv::module_emitter
    loom::target::facts_builder
    loom::target::function_contract
    loom::target::function_version
    loom::target::profile
    loom::target::types
    loom::testing::module_ptr
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    vulkan_profile_test
  SRCS
    "vulkan_profile_test.cc"
  DEPS
    ::vulkan_profile
    iree::base
    iree::hal
    iree::hal::drivers::vulkan::api
    iree::hal::drivers::vulkan::device_spec
    iree::hal::utils::device_spec_builder
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::arch::spirv::cooperative_properties
    loom::target::arch::spirv::features
    loom::target::arch::spirv::profile
    loom::target::types
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
iree_execution_test_suite(
  NAME
    iree_test_loom_execution_test
  MANIFESTS
    "test/iree-test-loom_spirv_vulkan.test.json"
  TOOLS
    "iree-test-loom=loom::tools::iree-test-loom"
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/execution/hal/testdata/buffer_bulk_hal_actual.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/execution/hal/testdata/dynamic_workgroup_hal_actual.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/execution/hal/testdata/launch_schedule_hal_actual.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/spirv/test/spirv_vulkan_coop_hal_actual.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/spirv/test/spirv_vulkan_coop_u8_hal_actual.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/spirv/test/spirv_vulkan_hal_actual.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/spirv/test/spirv_vulkan_integer_hal_actual.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/spirv/test/spirv_vulkan_quantized_hal_actual.loom"
  SANITIZER_SUPPRESSIONS
    lsan
    vulkan
  LABELS
    "iree-run-requirement=runtime.resource.vulkan_device"
    "requires-gpu-vulkan"
    "runtime-resource=vulkan-device"
  RESOURCE_GROUP
    loom-vulkan-tests
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
