################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/import/cxx/symbol/BUILD.bazel                                  #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_IMPORT_CXX)
loom_cc_library(
  NAME
    names
  HDRS
    "names.h"
  SRCS
    "names.cc"
  DEPS
    loom::import::cxx::source
  PUBLIC
)
endif()

if(LOOM_IMPORT_CXX)
loom_cc_library(
  NAME
    functions
  HDRS
    "functions.h"
  SRCS
    "functions.cc"
  DEPS
    ::names
    iree::third_party::cxx_parser
    loom::import::cxx::binding::config
    loom::import::cxx::binding::intrinsics
    loom::import::cxx::binding::launch
    loom::import::cxx::source
    loom::import::cxx::source::attributes
    loom::import::cxx::source::error
    loom::import::cxx::source::locations
    loom::import::cxx::value::types
    loom::ir
    loom::ops::check
    loom::ops::func
    loom::ops::kernel
  PUBLIC
)
endif()

if(LOOM_IMPORT_CXX)
loom_cc_test(
  NAME
    functions_test
  SRCS
    "functions_test.cc"
  DEPS
    ::functions
    iree::testing::gtest
    iree::testing::gtest_main
    iree::third_party::cxx_parser
    loom::import::cxx::value::builder_test
    loom::ops::check
    loom::ops::func
    loom::ops::kernel
  LABELS
    "iree-build-requirement=loom.import.cxx"
)
endif()

foreach(_target IN ITEMS
    loom_import_cxx_symbol_names.objects
    loom_import_cxx_symbol_functions.objects
    loom_import_cxx_symbol_functions_test
)
  if(TARGET ${_target})
    target_compile_features(${_target} PRIVATE cxx_std_23)
    if(MSVC)
      target_compile_options(${_target} PRIVATE /EHsc /GR)
    else()
      target_compile_options(${_target} PRIVATE -fexceptions -frtti)
    endif()
  endif()
endforeach()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
