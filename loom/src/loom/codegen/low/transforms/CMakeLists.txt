################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/codegen/low/transforms/BUILD.bazel                             #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

loom_cc_library(
  NAME
    allocation
  HDRS
    "allocation.h"
  SRCS
    "allocation.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::allocation_live_range_splitting
    loom::codegen::low::allocation_materialization
    loom::codegen::low::descriptors
    loom::codegen::low::function
    loom::codegen::low::function_model
    loom::codegen::low::pipeline::pass_environment
    loom::codegen::low::rematerialization
    loom::ir
    loom::ops::low
    loom::ops::type_registry
    loom::pass::pipeline
    loom::pass::registry
    loom::pass::types
    loom::target::function_version
  PUBLIC
)

loom_cc_library(
  NAME
    cfg_tuple_decomposition
  HDRS
    "cfg_tuple_decomposition.h"
  SRCS
    "cfg_tuple_decomposition.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::codegen::low::function
    loom::ir
    loom::ops::low
    loom::ops::op_defs
    loom::pass::types
    loom::pass::value_facts
    loom::rewrite::rewriter
    loom::target::registers
  PUBLIC
)

loom_cc_library(
  NAME
    dce
  HDRS
    "dce.h"
  SRCS
    "dce.c"
  DEPS
    iree::base
    loom::codegen::low::descriptors
    loom::codegen::low::function
    loom::codegen::low::pipeline::pass_environment
    loom::codegen::low::target_binding
    loom::error::emitter
    loom::ir
    loom::ops::low
    loom::ops::op_defs
    loom::pass::types
    loom::target::function_version
    loom::transforms::cleanup::dce
  PUBLIC
)

loom_cc_library(
  NAME
    operand_forms
  HDRS
    "operand_forms.h"
  SRCS
    "operand_forms.c"
  DEPS
    iree::base
    iree::base::internal
    iree::base::internal::arena
    loom::analysis::consumption
    loom::codegen::low::builder
    loom::codegen::low::descriptor_traits
    loom::codegen::low::descriptors
    loom::codegen::low::diagnostics
    loom::codegen::low::function
    loom::codegen::low::pipeline::pass_environment
    loom::codegen::low::storage_relation
    loom::codegen::low::target_binding
    loom::error::error_defs
    loom::ir
    loom::ops::low
    loom::ops::op_defs
    loom::pass::pipeline
    loom::pass::registry
    loom::pass::types
    loom::pass::value_facts
    loom::rewrite::materialize
    loom::rewrite::remap
    loom::rewrite::rewriter
    loom::target::function_version
  PUBLIC
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
