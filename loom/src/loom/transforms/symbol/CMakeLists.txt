################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/transforms/symbol/BUILD.bazel                                  #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

loom_cc_library(
  NAME
    template_selection
  HDRS
    "template_applicability.h"
    "template_selection.h"
  SRCS
    "template_applicability.c"
    "template_selection.c"
  DEPS
    ::symbol_pruning
    iree::base
    iree::base::internal::arena
    loom::analysis::condition_facts
    loom::analysis::symbol_facts
    loom::analysis::symbol_liveness
    loom::analysis::symbol_references
    loom::analysis::template_provider_catalog
    loom::error::error_defs
    loom::ir
    loom::ir::facts
    loom::ops::func
    loom::ops::op_defs
    loom::ops::scf
    loom::ops::target
    loom::ops::template
    loom::pass::pipeline
    loom::pass::registry
    loom::pass::report
    loom::pass::types
    loom::pass::value_facts
    loom::rewrite::rewriter
    loom::target::condition
    loom::target::facts
    loom::target::function_version
    loom::target::pass_environment
    loom::util::bstring
  PUBLIC
)

loom_cc_test(
  NAME
    template_selection_query_test
  SRCS
    "template_selection_query_test.cc"
  DEPS
    ::template_selection
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::analysis::symbol_facts
    loom::analysis::template_provider_catalog
    loom::format::text::parser
    loom::format::text::printer
    loom::ir
    loom::ops::func
    loom::ops::template
    loom::testing::module_ptr
)

loom_cc_library(
  NAME
    inline_callables
  HDRS
    "inline_callables.h"
  SRCS
    "inline_callables.c"
  DEPS
    iree::base
    loom::analysis::availability
    loom::analysis::scc
    loom::analysis::symbol_references
    loom::error::error_defs
    loom::ir
    loom::ops::func
    loom::ops::op_defs
    loom::pass::types
    loom::rewrite::callable
    loom::rewrite::rewriter
    loom::target::pass_environment
  PUBLIC
)

loom_cc_library(
  NAME
    refine_boundaries
  HDRS
    "refine_boundaries.h"
  SRCS
    "refine_boundaries.c"
  DEPS
    iree::base
    loom::analysis::scc
    loom::analysis::type_refinement
    loom::error::error_defs
    loom::ir
    loom::ir::facts
    loom::ir::type_refinement
    loom::ops::func
    loom::ops::op_defs
    loom::ops::special_values
    loom::ops::type_registry
    loom::pass::pipeline
    loom::pass::registry
    loom::pass::types
    loom::rewrite::materialize
    loom::rewrite::remap
    loom::transforms::cleanup::canonicalize
    loom::util::fact_table
    loom::util::walk
  PUBLIC
)

loom_cc_library(
  NAME
    symbol_pruning
  HDRS
    "symbol_pruning.h"
  SRCS
    "symbol_pruning.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::analysis::symbol_liveness
    loom::ir
    loom::ops::func
    loom::ops::kernel
    loom::ops::op_defs
  PUBLIC
)

loom_cc_library(
  NAME
    symbol_dce
  HDRS
    "symbol_dce.h"
  SRCS
    "symbol_dce.c"
  DEPS
    ::symbol_pruning
    iree::base
    loom::analysis::symbol_liveness
    loom::analysis::symbol_references
    loom::ir
    loom::pass::types
    loom::target::pass_environment
  PUBLIC
)

loom_cc_test(
  NAME
    symbol_dce_test
  SRCS
    "symbol_dce_test.cc"
  DEPS
    ::symbol_dce
    iree::base::internal::arena
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::text_asm
    loom::format::bytecode::reader
    loom::format::bytecode::writer
    loom::format::text::parser
    loom::format::text::printer
    loom::ir
    loom::ops::config
    loom::ops::func
    loom::ops::low
    loom::ops::target
    loom::ops::test
    loom::target::test::descriptors
    loom::testing::module_ptr
    loom::verify
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
