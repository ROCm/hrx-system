################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/arch/amdgpu/contracts/BUILD.bazel                       #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_contract_file_family(
  NAME
    contract_tables_gen
  GENERATOR
    loom::py::loom::gen::target::contracts::amdgpu_contract_table_family
  FRAGMENTS
    "arithmetic=amdgpu.arithmetic"
    "async=amdgpu.async"
    "buffer=amdgpu.buffer"
    "compare=amdgpu.compare"
    "config=amdgpu.config"
    "dot=amdgpu.dot"
    "integer=amdgpu.integer"
    "reduce=amdgpu.reduce"
    "sanitizer=amdgpu.sanitizer"
    "view=amdgpu.view"
  COMMENT
    "Generating AMDGPU contract table families"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_contract_cc_libraries(
  NAME
    arithmetic
  CONTRACT_DEPS
    loom::target::contract
  LOWER_RULE_DEPS
    loom::codegen::low::lower
    loom::ops::index
    loom::ops::scalar
    loom::ops::vector
    loom::target::arch::amdgpu::lower::kinds
    loom::target::arch::amdgpu::lower::materializers
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_contract_cc_libraries(
  NAME
    async
  CONTRACT_DEPS
    loom::target::contract
  LOWER_RULE_DEPS
    loom::codegen::low::lower
    loom::ops::kernel
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_contract_cc_libraries(
  NAME
    buffer
  CONTRACT_DEPS
    loom::target::contract
  LOWER_RULE_DEPS
    loom::codegen::low::lower
    loom::ops::buffer
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_contract_cc_libraries(
  NAME
    config
  CONTRACT_DEPS
    loom::target::contract
  LOWER_RULE_DEPS
    loom::codegen::low::lower
    loom::ops::config
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_contract_cc_libraries(
  NAME
    compare
  CONTRACT_DEPS
    loom::target::contract
  LOWER_RULE_DEPS
    loom::codegen::low::lower
    loom::ops::index
    loom::ops::scalar
    loom::target::arch::amdgpu::lower::materializers
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_contract_cc_libraries(
  NAME
    dot
  CONTRACT_DEPS
    loom::target::contract
  LOWER_RULE_DEPS
    loom::codegen::low::lower
    loom::ops::vector
    loom::target::arch::amdgpu::lower::kinds
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_contract_cc_libraries(
  NAME
    integer
  CONTRACT_DEPS
    loom::target::contract
  LOWER_RULE_DEPS
    loom::codegen::low::lower
    loom::ops::index
    loom::ops::scalar
    loom::target::arch::amdgpu::lower::materializers
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_table_cc_library(
  NAME
    matrix
  GENERATOR
    loom::py::loom::gen::target::contracts::amdgpu_contract_fragment
  ARGS
    "--contract-fragment=amdgpu.matrix"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_contract_cc_libraries(
  NAME
    reduce
  CONTRACT_DEPS
    loom::target::contract
  LOWER_RULE_DEPS
    loom::codegen::low::lower
    loom::ops::vector
    loom::target::arch::amdgpu::lower::kinds
    loom::target::arch::amdgpu::lower::materializers
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_contract_cc_libraries(
  NAME
    sanitizer
  CONTRACT_DEPS
    loom::target::contract
  LOWER_RULE_DEPS
    loom::codegen::low::lower
    loom::ops::sanitizer
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_contract_cc_libraries(
  NAME
    view
  CONTRACT_DEPS
    loom::target::contract
  LOWER_RULE_DEPS
    loom::codegen::low::lower
    loom::ops::view
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    contracts
  HDRS
    "arithmetic.h"
    "arithmetic_lower_rules.h"
    "async.h"
    "async_lower_rules.h"
    "buffer.h"
    "buffer_lower_rules.h"
    "compare.h"
    "compare_lower_rules.h"
    "config.h"
    "config_lower_rules.h"
    "dot.h"
    "dot_lower_rules.h"
    "integer.h"
    "integer_lower_rules.h"
    "matrix.h"
    "reduce.h"
    "reduce_lower_rules.h"
    "sanitizer.h"
    "sanitizer_lower_rules.h"
    "view.h"
    "view_lower_rules.h"
  DEPS
    ::arithmetic
    ::arithmetic_lower_rules
    ::async
    ::async_lower_rules
    ::buffer
    ::buffer_lower_rules
    ::compare
    ::compare_lower_rules
    ::config
    ::config_lower_rules
    ::dot
    ::dot_lower_rules
    ::integer
    ::integer_lower_rules
    ::matrix
    ::reduce
    ::reduce_lower_rules
    ::sanitizer
    ::sanitizer_lower_rules
    ::view
    ::view_lower_rules
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
