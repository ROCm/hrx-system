################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/arch/amdgpu/matrix/BUILD.bazel                          #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    types
  HDRS
    "types.h"
  DEPS
    iree::base
    loom::analysis::contract_roles
    loom::analysis::matrix_fragment_layout
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info_defs
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_target_table_cc_library(
  NAME
    contract_tables
  GENERATOR
    loom::py::loom::gen::target::arch::amdgpu::matrix::contract_tables
  ARGS
    "--public-header=loom/target/arch/amdgpu/matrix/contract_tables.h"
  DEPS
    ::types
    iree::base
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    contract
  HDRS
    "contract.h"
  SRCS
    "contract.c"
  DEPS
    ::contract_tables
    ::types
    iree::base
    loom::analysis::contract_roles
    loom::analysis::matrix_fragment_layout
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
    loom::target::arch::amdgpu::target_info_defs
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    projection
  HDRS
    "projection.h"
  SRCS
    "projection.c"
  DEPS
    ::contract
    loom::analysis::contract
    loom::analysis::contract_storage
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_test(
  NAME
    contract_test
  SRCS
    "contract_test.cc"
  DEPS
    ::contract
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::arch::amdgpu::refs::target_refs
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_test(
  NAME
    projection_test
  SRCS
    "projection_test.cc"
  DEPS
    ::contract
    ::projection
    iree::testing::gtest
    iree::testing::gtest_main
    loom::analysis::contract_storage
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
