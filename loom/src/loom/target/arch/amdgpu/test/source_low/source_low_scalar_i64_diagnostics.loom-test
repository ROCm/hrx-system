// RUN: emit source-low output=low

amdgpu.target<gfx1100> @target
func.def target(@target) @scalar_i64_remsi_dynamic_rejected(%lhs: i64, %rhs: i64) -> (i64) {
  // ERROR@+1: AMDGPU/023 {constraint_key="scalar_remsi.signed_i64_dynamic"}
  %remainder = scalar.remsi %lhs, %rhs : i64
  func.return %remainder : i64
}
