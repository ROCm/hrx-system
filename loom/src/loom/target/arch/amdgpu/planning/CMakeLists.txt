################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/arch/amdgpu/planning/BUILD.bazel                        #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    address_state
  HDRS
    "address_state.h"
  SRCS
    "address_state.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::descriptors
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::encoding
    loom::target::arch::amdgpu::refs::target_refs
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_test(
  NAME
    address_state_test
  SRCS
    "address_state_test.cc"
  DEPS
    ::address_state
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::allocation
    loom::codegen::low::descriptors
    loom::codegen::low::schedule
    loom::ir
    loom::target::arch::amdgpu::descriptors::low_registry
    loom::target::arch::amdgpu::encoding
    loom::target::arch::amdgpu::refs::target_refs
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    descriptor_semantics
  HDRS
    "descriptor_semantics.h"
  SRCS
    "descriptor_semantics.c"
  DEPS
    iree::base
    loom::codegen::low::schedule
    loom::target::arch::amdgpu::refs::target_refs
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    matrix_wait_states
  HDRS
    "matrix_wait_states.h"
  SRCS
    "matrix_wait_states.c"
  DEPS
    iree::base
    loom::target::arch::amdgpu::target_info_defs
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_test(
  NAME
    matrix_wait_states_test
  SRCS
    "matrix_wait_states_test.cc"
  DEPS
    ::matrix_wait_states
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    matrix_coexecution
  HDRS
    "matrix_coexecution.h"
  TEXTUAL_HDRS
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/matrix_coexecution_source_layouts.inl"
  SRCS
    "matrix_coexecution.c"
  DEPS
    ::descriptor_semantics
    ::structural_packet
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::target::arch::amdgpu::matrix::types
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
    loom::target::arch::amdgpu::target_info_defs
    loom::util::cfg_graph
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    structural_packet
  HDRS
    "structural_packet.h"
  SRCS
    "structural_packet.c"
  DEPS
    iree::base
    loom::codegen::low::allocation
    loom::codegen::low::allocation::move_topology
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::refs::target_refs
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    wait_states
  HDRS
    "wait_states.h"
  SRCS
    "wait_states.c"
  DEPS
    ::descriptor_semantics
    ::matrix_coexecution
    ::matrix_wait_states
    ::structural_packet
    ::vopd_plan
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::packet
    loom::codegen::low::packet_hazard_plan
    loom::codegen::low::packet_hazard_plan_json
    loom::codegen::low::packet_progress
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::encoding
    loom::target::arch::amdgpu::matrix::contract
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
    loom::util::json
    loom::util::stream
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    wait_counters
  HDRS
    "wait_counters.h"
  DEPS
    iree::base
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    wait_frontier
  HDRS
    "wait_frontier.h"
  SRCS
    "wait_frontier.c"
  DEPS
    ::wait_counters
    iree::base
    iree::base::internal
    iree::base::internal::arena
    loom::codegen::low::allocation::table
    loom::codegen::low::memory_access
    loom::codegen::low::schedule
    loom::target::arch::amdgpu::refs::target_refs
    loom::util::cfg_graph
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    wait_loop
  HDRS
    "wait_loop.h"
  SRCS
    "wait_loop.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::codegen::low::schedule
    loom::ops::low
    loom::util::cfg_graph
    loom::util::cfg_loop
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    wait_plan
  HDRS
    "wait_plan.h"
  SRCS
    "wait_plan.c"
  DEPS
    ::structural_packet
    ::wait_counters
    ::wait_frontier
    ::wait_loop
    ::wait_packet_tables
    iree::base
    iree::base::internal
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::allocation::storage
    loom::codegen::low::allocation::storage_lease
    loom::codegen::low::descriptors
    loom::codegen::low::packet
    loom::codegen::low::packet_hazard_plan
    loom::codegen::low::packet_hazard_plan_json
    loom::codegen::low::packet_progress
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_id
    loom::util::segmented_storage
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    wait_packet_tables
  HDRS
    "wait_packet_tables.h"
  TEXTUAL_HDRS
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/descriptors/wait_packet_descriptor_lookups.inl"
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/descriptors/wait_packet_descriptor_ranges.inl"
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/descriptors/wait_packet_descriptors.inl"
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/descriptors/wait_packet_immediates.inl"
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/descriptors/wait_packet_selections.inl"
  SRCS
    "wait_packet_tables.c"
  DEPS
    ::wait_counters
    iree::base
    loom::codegen::low::descriptors
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info_defs
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    wait_packets
  HDRS
    "wait_packets.h"
  SRCS
    "wait_packets.c"
  DEPS
    ::wait_packet_tables
    ::wait_plan
    iree::base
    iree::base::internal
    iree::base::internal::arena
    loom::codegen::low::descriptors
    loom::codegen::low::function
    loom::ir
    loom::target::arch::amdgpu::refs::target_refs
    loom::util::json
    loom::util::stream
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_test(
  NAME
    wait_packets_test
  SRCS
    "wait_packets_test.cc"
  DEPS
    ::wait_packets
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::descriptors
    loom::target::arch::amdgpu::descriptors::low_registry
    loom::target::arch::amdgpu::refs::target_refs
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    vopd_plan
  HDRS
    "vopd_plan.h"
  TEXTUAL_HDRS
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/descriptors/vopd_component_descriptor_lookup_ranges.inl"
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/descriptors/vopd_component_descriptor_lookups.inl"
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/descriptors/vopd_component_rules.inl"
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/descriptors/vopd_pair_affinities.inl"
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/descriptors/vopd_pair_affinity_ranges.inl"
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/descriptors/vopd_pair_placement_recipes.inl"
  SRCS
    "vopd_plan.c"
  DEPS
    ::address_state
    ::descriptor_semantics
    ::matrix_coexecution
    ::structural_packet
    ::wait_packets
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::diagnostics
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::encoding
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
    loom::util::json
    loom::util::stream
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    packet_plan
  HDRS
    "packet_plan.h"
  SRCS
    "packet_plan.c"
  DEPS
    ::address_state
    ::matrix_coexecution
    ::vopd_plan
    ::wait_packets
    ::wait_plan
    ::wait_states
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::schedule
    loom::target::arch::amdgpu::target_id
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    storage_lease
  HDRS
    "storage_lease.h"
  SRCS
    "storage_lease.c"
  DEPS
    iree::base
    loom::codegen::low::storage_lease
    loom::target::arch::amdgpu::target_info_defs
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    occupancy_model
  HDRS
    "occupancy_model.h"
  SRCS
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/amdgpu/occupancy_model_tables.c"
    "occupancy_model.c"
  DEPS
    iree::base
    loom::target::arch::amdgpu::target_info
    loom::target::residency
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    occupancy
  HDRS
    "occupancy.h"
  SRCS
    "occupancy.c"
  DEPS
    ::occupancy_model
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::diagnostics
    loom::error::error_defs
    loom::ir
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_id
    loom::target::arch::amdgpu::target_info_defs
    loom::target::launch
    loom::target::residency
    loom::target::types
    loom::util::json
    loom::util::stream
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_test(
  NAME
    occupancy_test
  SRCS
    "occupancy_test.cc"
  DEPS
    ::occupancy
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::arch::amdgpu::target_info
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
