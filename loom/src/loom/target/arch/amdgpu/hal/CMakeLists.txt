################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/arch/amdgpu/hal/BUILD.bazel                             #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    kernel_abi
  HDRS
    "binding_descriptor.h"
    "kernel_abi.h"
  SRCS
    "kernel_abi.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::function
    loom::codegen::low::target_binding
    loom::error::emitter
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::error_defs
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
    loom::target::registers
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    binding_materialization
  HDRS
    "binding_materialization.h"
  SRCS
    "binding_materialization.c"
  DEPS
    ::kernel_abi
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::builder
    loom::codegen::low::descriptors
    loom::codegen::low::function
    loom::ir
    loom::ops::low
    loom::rewrite::rewriter
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    binding_materialization_pass
  HDRS
    "binding_materialization_pass.h"
  SRCS
    "binding_materialization_pass.c"
  DEPS
    ::binding_materialization
    ::kernel_abi
    iree::base
    loom::codegen::low::function
    loom::codegen::low::pipeline::pass_environment
    loom::codegen::low::target_binding
    loom::ir
    loom::ops::low
    loom::pass::types
    loom::target::arch::amdgpu::ops
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
