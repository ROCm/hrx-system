################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/arch/spirv/contracts/BUILD.bazel                        #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_SPIRV)
loom_target_contract_file_family(
  NAME
    contract_tables_gen
  GENERATOR
    loom::py::loom::gen::target::contracts::spirv_contract_table_family
  FRAGMENTS
    "logical_core=spirv.logical.core"
  CONTRACT_SETS
    "spirv=logical_core"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV)
loom_target_contract_cc_libraries(
  NAME
    logical_core
  CONTRACT_DEPS
    loom::target::contract
  LOWER_RULE_DEPS
    loom::codegen::low::lower
    loom::ops::buffer
    loom::ops::index
    loom::ops::kernel
    loom::ops::scalar
    loom::ops::scf
    loom::ops::vector
    loom::ops::view
    loom::target::arch::spirv::descriptors::descriptors_ids
)
endif()

if(LOOM_TARGET_ARCH_SPIRV)
loom_cc_library(
  NAME
    sets
  HDRS
    "sets.h"
  SRCS
    "sets.c"
  DEPS
    ::logical_core
    ::logical_core_lower_rules
    loom::codegen::low::lower
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV)
loom_cc_library(
  NAME
    contracts
  HDRS
    "logical_core.h"
    "logical_core_lower_rules.h"
    "sets.h"
  DEPS
    ::logical_core
    ::logical_core_lower_rules
    ::sets
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
