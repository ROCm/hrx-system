// TEMPLATE: loom/src/loom/test/corpus/source_low/index_addressing.loom-test
// RUN: emit source-low output=low

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @index_madd_global_b32() {
  %c1 = index.constant 1 : index
  %c4 = index.constant 4 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c4, %c4, %c1) : index
} launch(%input: buffer, %output: buffer) {
  %x0 = kernel.workitem.id<x> : index
  %x = index.assume %x0 [range(%x0, 0, 3)] : index
  %y0 = kernel.workitem.id<y> : index
  %y = index.assume %y0 [range(%y0, 0, 3)] : index
  %zero = index.constant 0 : offset
  %row_stride = index.constant 4 : index
  %linear = index.madd %y, %row_stride, %x : index
  %input_view = buffer.view %input[%zero] : buffer -> view<16xi32, #dense>
  %output_view = buffer.view %output[%zero] : buffer -> view<16xi32, #dense>
  %loaded = vector.load %input_view[%linear] : view<16xi32, #dense> -> vector<1xi32>
  vector.store %loaded, %output_view[%linear] : vector<1xi32>, view<16xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(4, 4, 1) @index_madd_global_b32() asm<llvmir.generic.core> {
  %input_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %output_view = resource<hal_binding> {index = 1, source_type = hal.buffer} : reg<llvmir.ptr>
  %x = kernel.workitem_id.x
  %y = kernel.workitem_id.y
  %row_stride = const.i64 4
  %19 = mul.i64 %y, %row_stride
  %linear = add.i64 %19, %x
  %loaded = load.indexed.i32 %input_view, %linear, 0, 4
  store.indexed.i32 %loaded, %output_view, %linear, 0, 4
  return
}

// ====

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @index_madd_u24_global_b32() {
  %c1 = index.constant 1 : index
  %c4 = index.constant 4 : index
  %c8 = index.constant 8 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c8, %c4, %c1) : index
} launch(%input: buffer, %output: buffer) {
  %x0 = kernel.workitem.id<x> : index
  %x = index.assume %x0 [range(%x0, 0, 7)] : index
  %y0 = kernel.workitem.id<y> : index
  %y = index.assume %y0 [range(%y0, 0, 3)] : index
  %zero = index.constant 0 : offset
  %row_stride = index.constant 9 : index
  %linear = index.madd %y, %row_stride, %x : index
  %input_view = buffer.view %input[%zero] : buffer -> view<36xi32, #dense>
  %output_view = buffer.view %output[%zero] : buffer -> view<36xi32, #dense>
  %loaded = vector.load %input_view[%linear] : view<36xi32, #dense> -> vector<1xi32>
  vector.store %loaded, %output_view[%linear] : vector<1xi32>, view<36xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(8, 4, 1) @index_madd_u24_global_b32() asm<llvmir.generic.core> {
  %input_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %output_view = resource<hal_binding> {index = 1, source_type = hal.buffer} : reg<llvmir.ptr>
  %x = kernel.workitem_id.x
  %y = kernel.workitem_id.y
  %row_stride = const.i64 9
  %20 = mul.i64 %y, %row_stride
  %linear = add.i64 %20, %x
  %loaded = load.indexed.i32 %input_view, %linear, 0, 4
  store.indexed.i32 %loaded, %output_view, %linear, 0, 4
  return
}

// ====

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @index_madd_u24_literal_lhs_global_b32() {
  %c1 = index.constant 1 : index
  %c4 = index.constant 4 : index
  %c8 = index.constant 8 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c8, %c4, %c1) : index
} launch(%input: buffer, %output: buffer) {
  %x0 = kernel.workitem.id<x> : index
  %x = index.assume %x0 [range(%x0, 0, 7)] : index
  %y0 = kernel.workitem.id<y> : index
  %y = index.assume %y0 [range(%y0, 0, 3)] : index
  %zero = index.constant 0 : offset
  %row_stride = index.constant 9 : index
  %linear = index.madd %row_stride, %y, %x : index
  %input_view = buffer.view %input[%zero] : buffer -> view<36xi32, #dense>
  %output_view = buffer.view %output[%zero] : buffer -> view<36xi32, #dense>
  %loaded = vector.load %input_view[%linear] : view<36xi32, #dense> -> vector<1xi32>
  vector.store %loaded, %output_view[%linear] : vector<1xi32>, view<36xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(8, 4, 1) @index_madd_u24_literal_lhs_global_b32() asm<llvmir.generic.core> {
  %input_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %output_view = resource<hal_binding> {index = 1, source_type = hal.buffer} : reg<llvmir.ptr>
  %x = kernel.workitem_id.x
  %y = kernel.workitem_id.y
  %row_stride = const.i64 9
  %20 = mul.i64 %row_stride, %y
  %linear = add.i64 %20, %x
  %loaded = load.indexed.i32 %input_view, %linear, 0, 4
  store.indexed.i32 %loaded, %output_view, %linear, 0, 4
  return
}

// ====

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @index_madd_u24_literal_addend_global_b32() {
  %c1 = index.constant 1 : index
  %c4 = index.constant 4 : index
  %c8 = index.constant 8 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c8, %c4, %c1) : index
} launch(%input: buffer, %output: buffer) {
  %x0 = kernel.workitem.id<x> : index
  %x = index.assume %x0 [range(%x0, 0, 7)] : index
  %y0 = kernel.workitem.id<y> : index
  %y = index.assume %y0 [range(%y0, 0, 3)] : index
  %zero = index.constant 0 : offset
  %column = index.constant 1 : index
  %linear = index.madd %y, %x, %column : index
  %input_view = buffer.view %input[%zero] : buffer -> view<36xi32, #dense>
  %output_view = buffer.view %output[%zero] : buffer -> view<36xi32, #dense>
  %loaded = vector.load %input_view[%linear] : view<36xi32, #dense> -> vector<1xi32>
  vector.store %loaded, %output_view[%linear] : vector<1xi32>, view<36xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(8, 4, 1) @index_madd_u24_literal_addend_global_b32() asm<llvmir.generic.core> {
  %input_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %output_view = resource<hal_binding> {index = 1, source_type = hal.buffer} : reg<llvmir.ptr>
  %x = kernel.workitem_id.x
  %y = kernel.workitem_id.y
  %column = const.i64 1
  %20 = mul.i64 %y, %x
  %linear = add.i64 %20, %column
  %loaded = load.indexed.i32 %input_view, %linear, 4, 4
  store.indexed.i32 %loaded, %output_view, %linear, 4, 4
  return
}

// ====

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @index_madd_constant_addend_global_b32() {
  %c1 = index.constant 1 : index
  %c4 = index.constant 4 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c4, %c1, %c1) : index
} launch(%input: buffer, %output: buffer) {
  %x0 = kernel.workitem.id<x> : index
  %x = index.assume %x0 [range(%x0, 0, 3)] : index
  %zero = index.constant 0 : offset
  %row_stride = index.constant 4 : index
  %column = index.constant 1 : index
  %linear = index.madd %x, %row_stride, %column : index
  %input_view = buffer.view %input[%zero] : buffer -> view<16xi32, #dense>
  %output_view = buffer.view %output[%zero] : buffer -> view<16xi32, #dense>
  %loaded = vector.load %input_view[%linear] : view<16xi32, #dense> -> vector<1xi32>
  vector.store %loaded, %output_view[%linear] : vector<1xi32>, view<16xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(4, 1, 1) @index_madd_constant_addend_global_b32() asm<llvmir.generic.core> {
  %input_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %output_view = resource<hal_binding> {index = 1, source_type = hal.buffer} : reg<llvmir.ptr>
  %x = kernel.workitem_id.x
  %row_stride = const.i64 4
  %column = const.i64 1
  %18 = mul.i64 %x, %row_stride
  %linear = add.i64 %18, %column
  %loaded = load.indexed.i32 %input_view, %linear, 4, 16
  store.indexed.i32 %loaded, %output_view, %linear, 4, 16
  return
}

// ====

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @index_shli_global_b32() {
  %c1 = index.constant 1 : index
  %c4 = index.constant 4 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c4, %c1, %c1) : index
} launch(%input: buffer, %output: buffer) {
  %x0 = kernel.workitem.id<x> : index
  %x = index.assume %x0 [range(%x0, 0, 3)] : index
  %zero = index.constant 0 : offset
  %shift = index.constant 2 : index
  %linear = index.shli %x, %shift : index
  %input_view = buffer.view %input[%zero] : buffer -> view<16xi32, #dense>
  %output_view = buffer.view %output[%zero] : buffer -> view<16xi32, #dense>
  %loaded = vector.load %input_view[%linear] : view<16xi32, #dense> -> vector<1xi32>
  vector.store %loaded, %output_view[%linear] : vector<1xi32>, view<16xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(4, 1, 1) @index_shli_global_b32() asm<llvmir.generic.core> {
  %input_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %output_view = resource<hal_binding> {index = 1, source_type = hal.buffer} : reg<llvmir.ptr>
  %x = kernel.workitem_id.x
  %shift = const.i64 2
  %linear = shl.i64 %x, %shift
  %loaded = load.indexed.i32 %input_view, %linear, 0, 16
  store.indexed.i32 %loaded, %output_view, %linear, 0, 16
  return
}

// ====

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @index_shli_previous_row_global_b128() {
  %unit = index.constant 1 : index
  %active_lanes = index.constant 31 : index
  kernel.launch.config workgroups(%unit, %unit, %unit) workgroup_size(%active_lanes, %unit, %unit) : index
} launch(%input: buffer, %output: buffer) {
  %lane = kernel.workitem.id<x> : index
  %base_offset = index.constant 0 : offset
  %one = index.constant 1 : index
  %column = index.add %lane, %one : index
  %previous = index.sub %column, %one : index
  %shift = index.constant 2 : index
  %linear = index.shli %previous, %shift : index
  %input_view = buffer.view %input[%base_offset] : buffer -> view<128xi32, #dense>
  %output_view = buffer.view %output[%base_offset] : buffer -> view<128xi32, #dense>
  %loaded = vector.load %input_view[%linear] : view<128xi32, #dense> -> vector<4xi32>
  vector.store %loaded, %output_view[%linear] : vector<4xi32>, view<128xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(31, 1, 1) @index_shli_previous_row_global_b128() asm<llvmir.generic.core> {
  %input_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %output_view = resource<hal_binding> {index = 1, source_type = hal.buffer} : reg<llvmir.ptr>
  %lane = kernel.workitem_id.x
  %shift = const.i64 2
  %linear = shl.i64 %lane, %shift
  %loaded = load.indexed.v4i32 %input_view, %linear, 0, 16
  store.indexed.v4i32 %loaded, %output_view, %linear, 0, 16
  return
}

// ====

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @index_div_rem_power_of_two_global_b32() {
  %c1 = index.constant 1 : index
  %c32 = index.constant 32 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c32, %c1, %c1) : index
} launch(%input: buffer, %output: buffer) {
  %x0 = kernel.workitem.id<x> : index
  %x = index.assume %x0 [range(%x0, 0, 31)] : index
  %base_offset = index.constant 0 : offset
  %divisor = index.constant 8 : index
  %quotient = index.div %x, %divisor : index
  %remainder = index.rem %x, %divisor : index
  %input_view = buffer.view %input[%base_offset] : buffer -> view<4xi32, #dense>
  %output_view = buffer.view %output[%base_offset] : buffer -> view<8xi32, #dense>
  %loaded = vector.load %input_view[%quotient] : view<4xi32, #dense> -> vector<1xi32>
  vector.store %loaded, %output_view[%remainder] : vector<1xi32>, view<8xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(32, 1, 1) @index_div_rem_power_of_two_global_b32() asm<llvmir.generic.core> {
  %input_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %output_view = resource<hal_binding> {index = 1, source_type = hal.buffer} : reg<llvmir.ptr>
  %x = kernel.workitem_id.x
  %20 = const.i64 3
  %quotient = lshr.i64 %x, %20
  %22 = const.i64 7
  %remainder = and.i64 %x, %22
  %loaded = load.indexed.i32 %input_view, %quotient, 0, 4
  store.indexed.i32 %loaded, %output_view, %remainder, 0, 4
  return
}

// ====

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @index_div_rem_magic_mul_global_b32() {
  %c1 = index.constant 1 : index
  %c25 = index.constant 25 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c25, %c1, %c1) : index
} launch(%input: buffer, %output: buffer) {
  %x0 = kernel.workitem.id<x> : index
  %x = index.assume %x0 [range(%x0, 0, 24)] : index
  %base_offset = index.constant 0 : offset
  %divisor = index.constant 5 : index
  %quotient = index.div %x, %divisor : index
  %remainder = index.rem %x, %divisor : index
  %input_view = buffer.view %input[%base_offset] : buffer -> view<5xi32, #dense>
  %output_view = buffer.view %output[%base_offset] : buffer -> view<5xi32, #dense>
  %loaded = vector.load %input_view[%quotient] : view<5xi32, #dense> -> vector<1xi32>
  vector.store %loaded, %output_view[%remainder] : vector<1xi32>, view<5xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(25, 1, 1) @index_div_rem_magic_mul_global_b32() asm<llvmir.generic.core> {
  %input_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %output_view = resource<hal_binding> {index = 1, source_type = hal.buffer} : reg<llvmir.ptr>
  %x = kernel.workitem_id.x
  %divisor = const.i64 5
  %quotient = udiv.i64 %x, %divisor
  %remainder = urem.i64 %x, %divisor
  %loaded = load.indexed.i32 %input_view, %quotient, 0, 4
  store.indexed.i32 %loaded, %output_view, %remainder, 0, 4
  return
}

// ====

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @index_div_rem_magic_add_global_b32() {
  %c1 = index.constant 1 : index
  %c49 = index.constant 49 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c49, %c1, %c1) : index
} launch(%input: buffer, %output: buffer) {
  %x0 = kernel.workitem.id<x> : index
  %x = index.assume %x0 [range(%x0, 0, 48)] : index
  %base_offset = index.constant 0 : offset
  %divisor = index.constant 7 : index
  %quotient = index.div %x, %divisor : index
  %remainder = index.rem %x, %divisor : index
  %input_view = buffer.view %input[%base_offset] : buffer -> view<7xi32, #dense>
  %output_view = buffer.view %output[%base_offset] : buffer -> view<7xi32, #dense>
  %loaded = vector.load %input_view[%quotient] : view<7xi32, #dense> -> vector<1xi32>
  vector.store %loaded, %output_view[%remainder] : vector<1xi32>, view<7xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(49, 1, 1) @index_div_rem_magic_add_global_b32() asm<llvmir.generic.core> {
  %input_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %output_view = resource<hal_binding> {index = 1, source_type = hal.buffer} : reg<llvmir.ptr>
  %x = kernel.workitem_id.x
  %divisor = const.i64 7
  %quotient = udiv.i64 %x, %divisor
  %remainder = urem.i64 %x, %divisor
  %loaded = load.indexed.i32 %input_view, %quotient, 0, 4
  store.indexed.i32 %loaded, %output_view, %remainder, 0, 4
  return
}

// ====

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @workitem_y_lds_uses_explicit_vaddr() {
  %c1 = index.constant 1 : index
  %c64 = index.constant 64 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c1, %c64, %c1) : index
} launch(%output: buffer) {
  %y0 = kernel.workitem.id<y> : index
  %y = index.assume %y0 [range(%y0, 0, 63)] : index
  %zero = index.constant 0 : offset
  %bytes = index.constant 256 : offset
  %scratch = buffer.alloca %bytes {base_alignment = 16, memory_space = workgroup} : buffer
  %scratch_view = buffer.view %scratch[%zero] : buffer -> view<64xi32, #dense>
  %output_view = buffer.view %output[%zero] : buffer -> view<64xi32, #dense>
  %value = vector.constant 7 : vector<1xi32>
  vector.store %value, %scratch_view[%y] : vector<1xi32>, view<64xi32, #dense>
  %loaded = vector.load %scratch_view[%y] : view<64xi32, #dense> -> vector<1xi32>
  vector.store %loaded, %output_view[%y] : vector<1xi32>, view<64xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(1, 64, 1) @workitem_y_lds_uses_explicit_vaddr() asm<llvmir.generic.core> {
  %output_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %y = kernel.workitem_id.y
  %bytes = const.i64 256
  %scratch_view = alloca.workgroup.i8 %bytes, 16
  %value = const.i32 7
  store.indexed.i32 %value, %scratch_view, %y, 0, 4
  %loaded = load.indexed.i32 %scratch_view, %y, 0, 4
  store.indexed.i32 %loaded, %output_view, %y, 0, 4
  return
}

// ====

llvmir.target<object> @target {
  triple = "amdgcn-amd-amdhsa"
}
kernel.def target(@target) @dispatch_id_xy_global_b32() {
  %c1 = index.constant 1 : index
  %c8 = index.constant 8 : index
  %c4 = index.constant 4 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c8, %c4, %c1) : index
} launch(%output: buffer) {
  %dispatch_x0 = kernel.workitem.dispatch.id<x> : index
  %dispatch_x = index.assume %dispatch_x0 [range(%dispatch_x0, 0, 31)] : index
  %dispatch_y0 = kernel.workitem.dispatch.id<y> : index
  %dispatch_y = index.assume %dispatch_y0 [range(%dispatch_y0, 0, 15)] : index
  %row_stride = index.constant 32 : index
  %linear = index.madd %dispatch_y, %row_stride, %dispatch_x : index
  %zero = index.constant 0 : offset
  %output_view = buffer.view %output[%zero] : buffer -> view<512xi32, #dense>
  %one = vector.constant 1 : vector<1xi32>
  vector.store %one, %output_view[%linear] : vector<1xi32>, view<512xi32, #dense>
  kernel.return
}

// ----
low.kernel.def target(@target) workgroup_size(8, 4, 1) @dispatch_id_xy_global_b32() asm<llvmir.generic.core> {
  %output_view = resource<hal_binding> {index = 0, source_type = hal.buffer} : reg<llvmir.ptr>
  %dispatch_x = kernel.workitem_dispatch_id.x
  %dispatch_y = kernel.workitem_dispatch_id.y
  %row_stride = const.i64 32
  %17 = mul.i64 %dispatch_y, %row_stride
  %linear = add.i64 %17, %dispatch_x
  %one = const.i32 1
  store.indexed.i32 %one, %output_view, %linear, 0, 4
  return
}
