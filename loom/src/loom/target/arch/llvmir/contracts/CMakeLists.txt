################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/arch/llvmir/contracts/BUILD.bazel                       #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_LLVMIR)
loom_target_table_cc_library(
  NAME
    generic_core
  GENERATOR
    loom::py::loom::gen::target::contracts::llvmir_contract_fragment
  ARGS
    "--contract-fragment=llvmir.generic.core"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR)
loom_target_table_cc_library(
  NAME
    generic_core_lower_rules
  GENERATOR
    loom::py::loom::gen::target::contracts::llvmir_lower_rule_shard
  ARGS
    "--contract-fragment=llvmir.generic.core"
  DEPS
    loom::codegen::low::lower
    loom::ops::buffer
    loom::ops::index
    loom::ops::kernel
    loom::ops::scalar
    loom::ops::scf
    loom::ops::vector
    loom::ops::view
    loom::target::arch::llvmir::descriptors::descriptors_ids
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR)
loom_cc_library(
  NAME
    contracts
  HDRS
    "generic_core.h"
    "generic_core_lower_rules.h"
  DEPS
    ::generic_core
    ::generic_core_lower_rules
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
