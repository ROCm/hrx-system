################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/arch/wasm/BUILD.bazel                                   #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_WASM)
loom_cc_library(
  NAME
    math_policy
  HDRS
    "math_policy.h"
  TEXTUAL_HDRS
    "math_policy_tables.inl"
  SRCS
    "math_policy.c"
  DEPS
    iree::base
    loom::target::math_policy
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_WASM)
loom_cc_library(
  NAME
    low_verify
  HDRS
    "low_verify.h"
  SRCS
    "low_verify.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::codegen::low::diagnostics
    loom::codegen::low::verify
    loom::error::error_defs
    loom::ir
    loom::ops::low
    loom::target::arch::wasm::descriptors::descriptors_ids
    loom::target::registers
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_WASM)
loom_cc_library(
  NAME
    provider
  HDRS
    "provider.h"
  SRCS
    "provider.c"
  DEPS
    ::low_verify
    ::math_policy
    iree::base
    loom::target::arch::wasm::descriptors::low_registry
    loom::target::arch::wasm::ops
    loom::target::emit::wasm::lower
    loom::target::provider
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_WASM)
loom_cc_test(
  NAME
    registers_test
  SRCS
    "registers_test.cc"
  DEPS
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::target_binding
    loom::target::arch::wasm::descriptors
    loom::target::registers
  LABELS
    "iree-build-requirement=loom.target.arch.wasm"
)
endif()

if(LOOM_TARGET_ARCH_WASM)
iree_native_test(
  NAME
    "source_low"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/source_low/source_low.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/source_low/vector_v4.loom-test"
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_WASM)
iree_native_test(
  NAME
    "source_low_diagnostics"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/source_low/source_low_diagnostics.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/source_low/vector_v4.loom-test"
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_WASM)
iree_native_test(
  NAME
    "structured_allocation"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/source_low/structured_allocation.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/source_low/vector_v4.loom-test"
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_WASM)
iree_native_test(
  NAME
    "structured_control"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/source_low/structured_control.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/source_low/vector_v4.loom-test"
  LABELS
    "loom-check"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
