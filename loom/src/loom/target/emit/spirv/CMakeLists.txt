################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/emit/spirv/BUILD.bazel                                  #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    binary_format
  HDRS
    "binary_format.h"
  DEPS
    loom::target::arch::spirv::isa
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    binary_writer
  HDRS
    "binary_writer.h"
  SRCS
    "binary_writer.c"
  DEPS
    iree::base
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    module_builder
  HDRS
    "module_builder.h"
  SRCS
    "module_builder.c"
  DEPS
    ::binary_format
    ::binary_writer
    iree::base
    loom::target::arch::spirv::features
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    module_types
  HDRS
    "module_types.h"
  SRCS
    "module_types.c"
  DEPS
    ::binary_format
    ::module_builder
    iree::base
    iree::base::internal::arena
    loom::target::arch::spirv::scalar_types
    loom::target::arch::spirv::value_types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    module_values
  HDRS
    "module_values.h"
  SRCS
    "module_values.c"
  DEPS
    ::module_builder
    iree::base
    iree::base::internal::arena
    loom::ir
    loom::target::arch::spirv::value_types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    module_instructions
  HDRS
    "module_instructions.h"
  SRCS
    "module_instructions.c"
  DEPS
    ::binary_format
    ::module_builder
    iree::base
    loom::target::arch::spirv::isa
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    module_abi
  HDRS
    "module_abi.h"
  SRCS
    "module_abi.c"
  DEPS
    ::binary_format
    ::module_builder
    ::module_instructions
    ::module_types
    ::module_values
    iree::base
    iree::base::internal::arena
    loom::codegen::low::target_binding
    loom::ir
    loom::ops::low
    loom::target::arch::spirv::abi
    loom::target::arch::spirv::descriptors::descriptors_ids
    loom::target::arch::spirv::registers
    loom::target::arch::spirv::scalar_types
    loom::target::arch::spirv::value_types
    loom::target::registers
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    module_storage
  HDRS
    "module_storage.h"
  SRCS
    "module_storage.c"
  DEPS
    ::binary_format
    ::module_builder
    ::module_types
    ::module_values
    iree::base
    iree::base::internal::arena
    loom::ir
    loom::ops::low
    loom::target::arch::spirv::descriptors
    loom::target::arch::spirv::isa
    loom::target::arch::spirv::registers
    loom::target::arch::spirv::scalar_types
    loom::target::arch::spirv::value_types
    loom::target::registers
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    module_emitter
  HDRS
    "module_emitter.h"
  SRCS
    "module_emitter.c"
  DEPS
    ::binary_format
    ::module_abi
    ::module_builder
    ::module_instructions
    ::module_storage
    ::module_types
    ::module_values
    iree::base
    iree::base::internal::arena
    loom::analysis::symbol_facts
    loom::codegen::low::function
    loom::codegen::low::target_binding
    loom::error::emitter
    loom::ir
    loom::ops::low
    loom::target::arch::spirv::descriptors::descriptors_ids
    loom::target::arch::spirv::features
    loom::target::arch::spirv::module_contract
    loom::target::arch::spirv::packet_rows
    loom::target::low_descriptor_registry
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    loom_check
  HDRS
    "check/loom_check.h"
  SRCS
    "check/loom_check.c"
  DEPS
    ::module_emitter
    iree::base
    loom::target::entry_selection
    loom::target::tool::spirv
    loom::tooling::compile::pipeline
    loom::tools::loom-check::execute
  TESTONLY
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_library(
  NAME
    check_provider
  HDRS
    "check/provider.h"
  SRCS
    "check/provider.c"
  DEPS
    ::loom_check
    iree::base
    loom::tools::loom-check::provider
  TESTONLY
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    module_builder_test
  SRCS
    "module_builder_test.cc"
  DEPS
    ::binary_format
    ::module_builder
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::arch::spirv::features
    loom::target::arch::spirv::records::target_records
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    module_values_test
  SRCS
    "module_values_test.cc"
  DEPS
    ::module_builder
    ::module_values
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::ir
    loom::ops::op_defs
    loom::ops::test
    loom::target::arch::spirv::records::target_records
    loom::target::arch::spirv::scalar_types
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
iree_native_test(
  NAME
    "barrier"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/barrier.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  ENV
    "LOOM_SPIRV_DIS=$<TARGET_FILE:iree::third_party::spirv_dis>"
    "LOOM_SPIRV_VAL=$<TARGET_FILE:iree::third_party::spirv_val>"
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
iree_native_test(
  NAME
    "emit"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/emit.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  ENV
    "LOOM_SPIRV_DIS=$<TARGET_FILE:iree::third_party::spirv_dis>"
    "LOOM_SPIRV_VAL=$<TARGET_FILE:iree::third_party::spirv_val>"
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
iree_native_test(
  NAME
    "source_structured_control"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/source_structured_control.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  ENV
    "LOOM_SPIRV_DIS=$<TARGET_FILE:iree::third_party::spirv_dis>"
    "LOOM_SPIRV_VAL=$<TARGET_FILE:iree::third_party::spirv_val>"
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
iree_native_test(
  NAME
    "workgroup_storage"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/workgroup_storage.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  ENV
    "LOOM_SPIRV_DIS=$<TARGET_FILE:iree::third_party::spirv_dis>"
    "LOOM_SPIRV_VAL=$<TARGET_FILE:iree::third_party::spirv_val>"
  LABELS
    "loom-check"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
