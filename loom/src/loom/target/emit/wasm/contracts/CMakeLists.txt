################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/emit/wasm/contracts/BUILD.bazel                         #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_target_table_cc_library(
  NAME
    core_simd128
  GENERATOR
    loom::py::loom::gen::target::contracts::wasm_contract_fragment
  ARGS
    "--contract-fragment=wasm.core.simd128"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_target_table_cc_library(
  NAME
    core_simd128_lower_rules
  GENERATOR
    loom::py::loom::gen::target::contracts::wasm_lower_rule_shard
  ARGS
    "--contract-fragment=wasm.core.simd128"
  DEPS
    loom::codegen::low::lower
    loom::ops::buffer
    loom::ops::index
    loom::ops::scalar
    loom::ops::vector
    loom::target::arch::wasm::descriptors::descriptors_ids
)
endif()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_cc_library(
  NAME
    contracts
  HDRS
    "core_simd128.h"
    "core_simd128_lower_rules.h"
  DEPS
    ::core_simd128
    ::core_simd128_lower_rules
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
