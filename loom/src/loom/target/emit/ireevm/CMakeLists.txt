################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/emit/ireevm/BUILD.bazel                                 #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_IREE_VM AND LOOM_EMIT_IREE_VM)
loom_cc_library(
  NAME
    module_plan
  HDRS
    "module_plan.h"
  SRCS
    "module_plan.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::analysis::symbol_facts
    loom::error::emitter
    loom::ir
    loom::ops::func_symbol_facts
    loom::ops::low
    loom::target::arch::ireevm::descriptors
    loom::target::function_contract
    loom::target::registers
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM AND LOOM_EMIT_IREE_VM)
loom_cc_library(
  NAME
    function_bytecode
  HDRS
    "function_bytecode.h"
  SRCS
    "function_bytecode.c"
  DEPS
    ::module_plan
    iree::base
    loom::codegen::low::allocation
    loom::codegen::low::descriptors
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::ireevm::descriptors
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM AND LOOM_EMIT_IREE_VM)
loom_cc_library(
  NAME
    module_archive
  HDRS
    "module_archive.h"
  SRCS
    "module_archive.c"
  DEPS
    ::function_bytecode
    iree::base
    iree::base::internal::flatcc::building
    iree::schemas::bytecode_module_def_c_fbs
    iree::vm::bytecode::isa
    iree::vm::bytecode::utils
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM AND LOOM_EMIT_IREE_VM)
loom_cc_library(
  NAME
    archive_emitter
  HDRS
    "archive_emitter.h"
  SRCS
    "archive_emitter.c"
  DEPS
    ::function_bytecode
    ::module_archive
    ::module_plan
    iree::base
    iree::base::internal::arena
    loom::analysis::symbol_facts
    loom::codegen::low::frame
    loom::codegen::low::verify
    loom::error::diagnostic
    loom::ir
    loom::ops::func
    loom::ops::low
    loom::ops::op_defs
    loom::ops::target
    loom::target::arch::ireevm::descriptors
    loom::target::arch::ireevm::provider
    loom::target::compile_report
    loom::target::compile_report_low
    loom::target::entry_selection
    loom::target::provider
    loom::target::registers
    loom::verify
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
