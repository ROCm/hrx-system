################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/emit/native/x86/BUILD.bazel                             #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_X86)
loom_cc_library(
  NAME
    assembly
  HDRS
    "assembly.h"
  SRCS
    "assembly.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::x86::descriptors::avx2_descriptors_ids
    loom::target::arch::x86::descriptors::avx512_descriptors_ids
    loom::target::arch::x86::descriptors::avx512_packed_dot_descriptors_ids
    loom::target::arch::x86::descriptors::packed_dot_descriptors_ids
    loom::target::arch::x86::descriptors::scalar_descriptors_ids
    loom::target::arch::x86::descriptors::simd128_descriptors_ids
    loom::target::arch::x86::register_classes
    loom::target::emit::native::assembly
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_X86)
loom_cc_library(
  NAME
    function
  HDRS
    "${IREE_BINARY_DIR}/loom/src/loom/target/arch/x86/descriptors/encoding_defs.h"
    "function.h"
  SRCS
    "function.c"
  DEPS
    iree::base
    iree::base::internal
    iree::base::internal::arena
    loom::codegen::low::frame
    loom::codegen::low::packet
    loom::codegen::low::storage_layout
    loom::ir
    loom::ops::low
    loom::target::arch::x86::descriptors::avx512_packed_dot_descriptors
    loom::target::arch::x86::register_classes
    loom::target::arch::x86::sysv_frame
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_X86)
loom_cc_test(
  NAME
    function_test
  SRCS
    "function_test.cc"
  DEPS
    ::function
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::frame
    loom::codegen::low::text_asm
    loom::format::text::parser
    loom::ir
    loom::ops::low
    loom::target::arch::x86::descriptors::low_registry
    loom::target::arch::x86::ops
    loom::target::arch::x86::sysv_abi
    loom::target::arch::x86::sysv_frame
    loom::testing::context
    loom::testing::module_ptr
  LABELS
    "iree-build-requirement=loom.target.arch.x86"
)
endif()

if(LOOM_TARGET_ARCH_X86)
loom_cc_library(
  NAME
    object
  HDRS
    "object.h"
  SRCS
    "object.c"
  DEPS
    ::function
    iree::base
    iree::base::internal::arena
    iree::io::stream
    loom::codegen::low::frame
    loom::ir
    loom::ops::low
    loom::ops::op_defs
    loom::target::arch::x86::ops
    loom::target::arch::x86::sysv_frame
    loom::target::emit::native::contribution
    loom::target::emit::native::elf_object
    loom::target::emit::native::object
    loom::target::function_version
    loom::target::provider
    loom::target::reporting::low
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_X86)
loom_cc_test(
  NAME
    object_test
  SRCS
    "object_test.cc"
  DEPS
    ::object
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::text_asm
    loom::format::text::parser
    loom::ir
    loom::ops::low
    loom::ops::op_defs
    loom::target::arch::x86::descriptors::low_registry
    loom::target::arch::x86::ops
    loom::target::function_version
    loom::target::provider
    loom::testing::context
    loom::testing::module_ptr
  LABELS
    "iree-build-requirement=loom.target.arch.x86"
)
endif()

if(LOOM_TARGET_ARCH_X86)
loom_cc_test(
  NAME
    direct_call_link_test
  SRCS
    "direct_call_link_test.c"
    "direct_call_object.stamp"
  DEPS

  LABELS
    "iree-build-requirement=loom.target.arch.x86"
)
endif()

if(LOOM_TARGET_ARCH_X86)
loom_cc_library(
  NAME
    loom_check
  HDRS
    "check/loom_check.h"
  SRCS
    "check/loom_check.c"
  DEPS
    ::assembly
    iree::base
    loom::tools::loom-check::execute
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_X86)
loom_check_test_suite(
  NAME
    loom_check_file_test
  SRCS
    "test/assembly.loom-test"
    "test/copies.loom-test"
    "test/high_product.loom-test"
    "test/lanes.loom-test"
    "test/schedule_scopes.loom-test"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
