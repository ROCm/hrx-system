################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/emit/native/amdgpu/BUILD.bazel                          #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    register_class
  HDRS
    "register_class.h"
  DEPS
    loom::codegen::low::descriptors
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    assembly
  HDRS
    "assembly.h"
  SRCS
    "assembly.c"
  DEPS
    ::register_class
    ::storage_layout
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::descriptors
    loom::codegen::low::move_sequence
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::encoding
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
    loom::target::emit::native::assembly
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    loom_check
  HDRS
    "check/loom_check.h"
  SRCS
    "check/loom_check.c"
  DEPS
    ::assembly
    ::encoding
    ::spill_lowering
    iree::base
    loom::codegen::low::frame
    loom::codegen::low::target_binding
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::planning::address_state
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::arch::amdgpu::planning::storage_lease
    loom::tools::loom-check::execute
  TESTONLY
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
iree_native_test(
  NAME
    "test_assembly"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/assembly.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
iree_native_test(
  NAME
    "test_assembly_wmma"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/assembly_wmma.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
iree_native_test(
  NAME
    "test_wait_plan"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/wait_plan.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
iree_native_test(
  NAME
    "test_wait_states"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/wait_states.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    metadata
  HDRS
    "metadata.h"
  SRCS
    "metadata.c"
  DEPS
    iree::base
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    descriptor
  HDRS
    "descriptor.h"
  SRCS
    "descriptor.c"
  DEPS
    ::metadata
    iree::base
    loom::target::arch::amdgpu::target_info
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    storage_layout
  HDRS
    "storage_layout.h"
  SRCS
    "storage_layout.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::codegen::low::storage_layout
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    storage_layout_test
  SRCS
    "storage_layout_test.cc"
  DEPS
    ::storage_layout
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::ir
    loom::ops::low
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    spill_lowering
  HDRS
    "spill_lowering.h"
  SRCS
    "spill_lowering.c"
  DEPS
    ::storage_layout
    iree::base
    iree::base::internal::arena
    loom::codegen::low::builder
    loom::codegen::low::descriptors
    loom::codegen::low::function
    loom::ir
    loom::ops::low
    loom::rewrite::rewriter
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::registers
  PUBLIC
)
endif()

if(LOOM_TARGET_AMDGPU_DESCRIPTOR_SET_RDNA3_CORE AND LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    spill_lowering_test
  SRCS
    "spill_lowering_test.cc"
  DEPS
    ::spill_lowering
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::builder
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::descriptors::low_registry
    loom::target::arch::amdgpu::refs::target_refs
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    encoding
  HDRS
    "encoding.h"
  SRCS
    "encoding.c"
  DEPS
    ::register_class
    ::storage_layout
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::descriptors
    loom::codegen::low::move_sequence
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::encoding
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
    loom::target::emit::native::fragment
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    kernel_record
  HDRS
    "kernel_record.h"
  SRCS
    "kernel_record.c"
  DEPS
    ::descriptor
    ::metadata
    ::preflight
    ::storage_layout
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::function
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::hal::kernel_abi
    loom::target::arch::amdgpu::target_id
    loom::target::arch::amdgpu::target_info
    loom::target::launch
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    preflight
  HDRS
    "preflight.h"
  SRCS
    "preflight.c"
  DEPS
    ::register_class
    iree::base
    loom::codegen::low::allocation
    loom::codegen::low::schedule
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::emit::native::fragment
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    kernel_assembly
  HDRS
    "kernel_assembly.h"
  SRCS
    "kernel_assembly.c"
  DEPS
    ::assembly
    ::kernel_record
    ::metadata
    iree::base
    loom::codegen::low::allocation
    loom::codegen::low::schedule
    loom::target::arch::amdgpu::hal::kernel_abi
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    kernel_hsaco
  HDRS
    "kernel_hsaco.h"
  SRCS
    "kernel_hsaco.c"
  DEPS
    ::encoding
    ::hsaco
    ::kernel_record
    iree::base
    iree::base::internal::arena
    iree::io::stream
    loom::codegen::low::allocation
    loom::codegen::low::schedule
    loom::target::arch::amdgpu::hal::kernel_abi
    loom::target::arch::amdgpu::planning::packet_plan
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    hsaco
  HDRS
    "hsaco.h"
  SRCS
    "hsaco.c"
  DEPS
    ::descriptor
    ::metadata
    iree::base
    iree::base::internal::arena
    iree::io::stream
    loom::target::arch::amdgpu::target_info
    loom::target::emit::native::elf
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    hal_kernel_library
  HDRS
    "hal_kernel_library.h"
  SRCS
    "hal_kernel_library.c"
  DEPS
    ::kernel_assembly
    ::kernel_hsaco
    ::preflight
    ::spill_lowering
    iree::base
    iree::base::internal::arena
    iree::io::stream
    loom::analysis::symbol_facts
    loom::codegen::low::allocation_materialization
    loom::codegen::low::frame
    loom::codegen::low::verify
    loom::error::diagnostic
    loom::error::error_defs
    loom::ir
    loom::ops::func_symbol_facts
    loom::ops::low
    loom::ops::target
    loom::target::arch::amdgpu::error_defs
    loom::target::arch::amdgpu::hal::kernel_abi
    loom::target::arch::amdgpu::ops
    loom::target::arch::amdgpu::planning::address_state
    loom::target::arch::amdgpu::planning::occupancy
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::arch::amdgpu::planning::storage_lease
    loom::target::arch::amdgpu::provider
    loom::target::arch::amdgpu::target_info
    loom::target::compile_report
    loom::target::compile_report_low
    loom::target::entry_selection
    loom::target::function_contract
    loom::target::launch
    loom::target::provider
    loom::target::types
    loom::verify
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    hal_kernel_library_test
  SRCS
    "hal_kernel_library_test.cc"
  DEPS
    ::hal_kernel_library
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::text_asm
    loom::error::error_defs
    loom::format::text::parser
    loom::ir
    loom::ops::op_registry
    loom::target::arch::amdgpu::descriptors::low_registry
    loom::target::arch::amdgpu::error_defs
    loom::target::arch::amdgpu::ops
    loom::target::arch::amdgpu::records::target_records
    loom::target::arch::amdgpu::target_info
    loom::testing::diagnostic_matchers
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    metadata_test
  SRCS
    "metadata_test.cc"
  DEPS
    ::metadata
    iree::base
    iree::base::internal::arena
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::emit::native::elf
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    descriptor_test
  SRCS
    "descriptor_test.cc"
  DEPS
    ::descriptor
    ::metadata
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    hsaco_test
  SRCS
    "hsaco_test.cc"
  DEPS
    ::descriptor
    ::hsaco
    ::metadata
    iree::base
    iree::base::internal::arena
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::arch::amdgpu::target_info
    loom::target::emit::native::elf
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    kernel_hsaco_test
  SRCS
    "kernel_hsaco_test.cc"
  DEPS
    ::kernel_hsaco
    iree::base
    iree::base::internal::arena
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::emit::native::elf
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    hsaco_hsa_test
  SRCS
    "hsaco_hsa_test.cc"
  DEPS
    ::kernel_hsaco
    iree::base
    iree::base::internal
    iree::base::internal::arena
    iree::base::internal::dynamic_library
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
    iree::third_party::hsa_runtime_headers
    loom::codegen::low::descriptors
    loom::codegen::low::frame
    loom::codegen::low::function
    loom::codegen::low::target_binding
    loom::codegen::low::text_asm
    loom::codegen::low::verify
    loom::error::diagnostic
    loom::format::text::parser
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::descriptors::low_registry
    loom::target::arch::amdgpu::hal::binding_materialization
    loom::target::arch::amdgpu::hal::kernel_abi
    loom::target::arch::amdgpu::ops
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::arch::amdgpu::planning::storage_lease
    loom::target::arch::amdgpu::records::target_records
    loom::target::arch::amdgpu::target_info
    loom::target::low_descriptor_registry
  LABELS
    "nodocker"
    "requires-gpu-amd"
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
