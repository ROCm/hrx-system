################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/emit/native/amdgpu/BUILD.bazel                          #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    register_class
  HDRS
    "register_class.h"
  DEPS
    loom::codegen::low::descriptors
    loom::target::arch::amdgpu::refs::target_refs
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    branch_layout
  HDRS
    "branch_layout.h"
  SRCS
    "branch_layout.c"
  DEPS
    iree::base
    iree::base::internal::arena
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    branch_layout_test
  SRCS
    "branch_layout_test.cc"
  DEPS
    ::branch_layout
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    assembly
  HDRS
    "assembly.h"
  SRCS
    "assembly.c"
  DEPS
    ::branch_layout
    ::register_class
    ::storage_layout
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::descriptors
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::encoding
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
    loom::target::emit::native::assembly
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    loom_check
  HDRS
    "check/loom_check.h"
  SRCS
    "check/loom_check.c"
  DEPS
    ::assembly
    ::encoding
    ::hal_kernel_library
    ::spill_lowering
    iree::base
    iree::base::core_headers
    loom::codegen::low::allocation_json
    loom::codegen::low::frame
    loom::codegen::low::packet_json
    loom::codegen::low::target_binding
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::planning::descriptor_semantics
    loom::target::arch::amdgpu::planning::occupancy
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::arch::amdgpu::planning::storage_lease
    loom::target::arch::amdgpu::planning::vopd_plan
    loom::tools::loom-check::execute
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_check_test_suite(
  NAME
    loom_check_file_test
  SRCS
    "test/allocation_json.loom-test"
    "test/assembly.loom-test"
    "test/assembly_allocation.loom-test"
    "test/assembly_cluster.loom-test"
    "test/assembly_q8.loom-test"
    "test/assembly_vopd_profiles.loom-test"
    "test/assembly_wmma.loom-test"
    "test/gfx1201_native.loom-test"
    "test/hal_kernel_library.loom-test"
    "test/packet.loom-test"
    "test/packet_json.loom-test"
    "test/spill_lowering.loom-test"
    "test/vopd_plan.loom-test"
    "test/wait_plan.loom-test"
    "test/wait_states.loom-test"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    metadata
  HDRS
    "metadata.h"
  SRCS
    "metadata.c"
  DEPS
    iree::base
    loom::target::arch::amdgpu::amdhsa_target_id
    loom::target::arch::amdgpu::target_info
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    descriptor
  HDRS
    "descriptor.h"
  SRCS
    "descriptor.c"
  DEPS
    ::metadata
    iree::base
    loom::target::arch::amdgpu::target_info
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    storage_layout
  HDRS
    "storage_layout.h"
  SRCS
    "storage_layout.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::codegen::low::storage_layout
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    storage_layout_test
  SRCS
    "storage_layout_test.cc"
  DEPS
    ::storage_layout
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::storage_layout
    loom::ir
    loom::ops::low
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    spill_lowering
  HDRS
    "spill_lowering.h"
  SRCS
    "spill_lowering.c"
  DEPS
    ::storage_layout
    iree::base
    iree::base::internal::arena
    loom::codegen::low::builder
    loom::codegen::low::descriptors
    loom::codegen::low::diagnostics
    loom::codegen::low::function
    loom::error::emitter
    loom::ir
    loom::ops::low
    loom::ops::type_registry
    loom::rewrite::rewriter
    loom::target::arch::amdgpu::error_defs
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::registers
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    text_fixup
  HDRS
    "text_fixup.h"
  DEPS
    iree::base
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    encoding
  HDRS
    "encoding.h"
  SRCS
    "encoding.c"
  DEPS
    ::branch_layout
    ::register_class
    ::storage_layout
    ::text_fixup
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::descriptors
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::encoding
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
  PUBLIC
)
endif()

iree_c_embed_data(
  NAME
    packet_plan_attention_bf16
  SRCS
    "packet_plan_attention_bf16.loom"
  C_FILE_OUTPUT
    "packet_plan_attention_bf16.c"
  H_FILE_OUTPUT
    "packet_plan_attention_bf16.h"
  IDENTIFIER
    "packet_plan_attention_bf16"
  TESTONLY
  FLATTEN
  PUBLIC
)

if(LOOM_TARGET_AMDGPU_DESCRIPTOR_SET_RDNA4_GFX125X_CORE AND LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_benchmark(
  NAME
    packet_plan_benchmark
  SRCS
    "packet_plan_benchmark.cc"
  DEPS
    ::packet_plan_attention_bf16
    iree::base
    iree::base::internal::arena
    iree::testing::benchmark_main
    loom::codegen::low::frame
    loom::codegen::low::function
    loom::codegen::low::target_binding
    loom::codegen::low::text_asm
    loom::codegen::low::verify
    loom::error::diagnostic
    loom::format::text::parser
    loom::ir
    loom::ops::low
    loom::ops::op_registry
    loom::target::arch::amdgpu::hal::kernel_abi
    loom::target::arch::amdgpu::planning::descriptor_semantics
    loom::target::arch::amdgpu::planning::occupancy
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::arch::amdgpu::planning::storage_lease
    loom::target::arch::amdgpu::planning::vopd_plan
    loom::target::arch::amdgpu::provider
    loom::target::low_descriptor_registry
    loom::target::provider
    loom::tooling::compile::pipeline
  TESTONLY
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    kernel_record
  HDRS
    "kernel_record.h"
  SRCS
    "kernel_record.c"
  DEPS
    ::descriptor
    ::kernel_entry
    ::metadata
    ::preflight
    ::storage_layout
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::function
    loom::codegen::low::packet
    loom::codegen::low::schedule
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::amdhsa_target_id
    loom::target::arch::amdgpu::artifact_key
    loom::target::arch::amdgpu::facts
    loom::target::arch::amdgpu::hal::kernel_abi
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
    loom::target::launch
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    preflight
  HDRS
    "preflight.h"
  SRCS
    "preflight.c"
  DEPS
    ::register_class
    iree::base
    loom::codegen::low::allocation
    loom::codegen::low::diagnostics
    loom::codegen::low::schedule
    loom::codegen::low::storage_layout
    loom::error::emitter
    loom::error::error_defs
    loom::ops::type_registry
    loom::target::arch::amdgpu::error_defs
    loom::target::arch::amdgpu::refs::target_refs
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    preflight_test
  SRCS
    "preflight_test.cc"
  DEPS
    ::preflight
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::descriptors
    loom::codegen::low::diagnostics
    loom::codegen::low::storage_layout
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::descriptors::low_registry
    loom::target::arch::amdgpu::error_defs
    loom::target::arch::amdgpu::facts
    loom::target::arch::amdgpu::records::target_records
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info_defs
    loom::target::facts_builder
    loom::testing::diagnostic_matchers
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    kernel_entry
  HDRS
    "kernel_entry.h"
  SRCS
    "kernel_entry.c"
  DEPS
    ::text_fixup
    iree::base
    iree::base::internal::arena
    loom::target::arch::amdgpu::target_info_defs
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    kernel_assembly
  HDRS
    "kernel_assembly.h"
  SRCS
    "kernel_assembly.c"
  DEPS
    ::assembly
    ::kernel_entry
    ::kernel_record
    ::metadata
    iree::base
    loom::codegen::low::allocation
    loom::codegen::low::schedule
    loom::target::arch::amdgpu::hal::kernel_abi
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    kernel_entry_test
  SRCS
    "kernel_entry_test.cc"
  DEPS
    ::kernel_entry
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    kernel_hsaco
  HDRS
    "kernel_hsaco.h"
  SRCS
    "kernel_hsaco.c"
  DEPS
    ::encoding
    ::hsaco
    ::kernel_entry
    ::kernel_record
    iree::base
    iree::base::internal::arena
    iree::io::stream
    loom::codegen::low::allocation
    loom::codegen::low::schedule
    loom::target::arch::amdgpu::hal::kernel_abi
    loom::target::arch::amdgpu::planning::occupancy
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::residency
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    kernel_emission
  HDRS
    "kernel_emission.h"
  SRCS
    "kernel_emission.c"
  DEPS
    ::kernel_assembly
    ::kernel_hsaco
    ::preflight
    iree::base
    iree::base::internal::arena
    loom::codegen::low::frame
    loom::ir
    loom::target::arch::amdgpu::hal::kernel_abi
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::arch::amdgpu::planning::wait_counters
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::reporting::low
    loom::target::reporting::report
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    hsaco
  HDRS
    "hsaco.h"
  SRCS
    "hsaco.c"
  DEPS
    ::descriptor
    ::metadata
    ::text_fixup
    iree::base
    iree::base::internal::arena
    iree::io::stream
    loom::target::arch::amdgpu::amdhsa_target_id
    loom::target::arch::amdgpu::target_info
    loom::target::emit::native::elf
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    runtime_globals
  HDRS
    "runtime_globals.h"
  SRCS
    "runtime_globals.c"
  DEPS
    ::hsaco
    iree::base
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    runtime_globals_test
  SRCS
    "runtime_globals_test.cc"
  DEPS
    ::runtime_globals
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    product_contract
  HDRS
    "product_contract.h"
  SRCS
    "product_contract.c"
  DEPS
    loom::target::product_contract
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_library(
  NAME
    hal_kernel_library
  HDRS
    "hal_kernel_library.h"
  SRCS
    "hal_kernel_library.c"
  DEPS
    ::kernel_emission
    ::kernel_hsaco
    ::preflight
    ::runtime_globals
    ::spill_lowering
    iree::base
    iree::base::internal::arena
    iree::io::stream
    loom::analysis::symbol_facts
    loom::codegen::low::allocation_materialization
    loom::codegen::low::diagnostics
    loom::codegen::low::frame
    loom::codegen::low::storage_layout
    loom::codegen::low::target_binding
    loom::codegen::low::verify
    loom::error::diagnostic
    loom::error::error_defs
    loom::ir
    loom::ops::global
    loom::ops::low
    loom::target::arch::amdgpu::amdhsa_target_id
    loom::target::arch::amdgpu::artifact_key
    loom::target::arch::amdgpu::facts
    loom::target::arch::amdgpu::hal::kernel_abi
    loom::target::arch::amdgpu::matrix::contract
    loom::target::arch::amdgpu::planning::descriptor_semantics
    loom::target::arch::amdgpu::planning::occupancy
    loom::target::arch::amdgpu::planning::storage_lease
    loom::target::arch::amdgpu::planning::vopd_plan
    loom::target::arch::amdgpu::provider
    loom::target::arch::amdgpu::refs::target_refs
    loom::target::arch::amdgpu::target_info
    loom::target::entry_selection
    loom::target::provider
    loom::target::reporting::artifact_manifest_collect
    loom::target::reporting::low
    loom::target::reporting::report
    loom::target::types
    loom::verify
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    hal_kernel_library_test
  SRCS
    "hal_kernel_library_test.cc"
  DEPS
    ::hal_kernel_library
    ::product_contract
    ::runtime_globals
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::analysis::symbol_facts
    loom::codegen::low::text_asm
    loom::error::error_defs
    loom::format::text::parser
    loom::ir
    loom::ops::func_symbol_facts
    loom::ops::op_registry
    loom::ops::sanitizer
    loom::ops::target
    loom::sanitizer::site_table
    loom::target::arch::amdgpu::amdhsa_target_id
    loom::target::arch::amdgpu::artifact_key
    loom::target::arch::amdgpu::descriptors::low_registry
    loom::target::arch::amdgpu::matrix::contract
    loom::target::arch::amdgpu::planning::wait_counters
    loom::target::arch::amdgpu::profile
    loom::target::arch::amdgpu::provider
    loom::target::arch::amdgpu::records::target_records
    loom::target::arch::amdgpu::target_info
    loom::target::emit::native::elf
    loom::target::facts_builder
    loom::target::function_contract
    loom::target::function_version
    loom::target::profile
    loom::testing::diagnostic_matchers
    loom::tooling::compile::pipeline
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    metadata_test
  SRCS
    "metadata_test.cc"
  DEPS
    ::metadata
    iree::base
    iree::base::internal::arena
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::emit::native::elf
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    descriptor_test
  SRCS
    "descriptor_test.cc"
  DEPS
    ::descriptor
    ::metadata
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::arch::amdgpu::target_info
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    hsaco_test
  SRCS
    "hsaco_test.cc"
  DEPS
    ::descriptor
    ::hsaco
    ::metadata
    iree::base
    iree::base::internal::arena
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::arch::amdgpu::amdhsa_target_id
    loom::target::arch::amdgpu::profile
    loom::target::arch::amdgpu::target_info
    loom::target::emit::native::elf
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    kernel_hsaco_test
  SRCS
    "kernel_hsaco_test.cc"
  DEPS
    ::kernel_hsaco
    iree::base
    iree::base::internal::arena
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::emit::native::elf
  LABELS
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    hsaco_hsa_test
  SRCS
    "hsaco_hsa_test.cc"
  DEPS
    ::kernel_hsaco
    ::product_contract
    iree::base
    iree::base::internal
    iree::base::internal::arena
    iree::base::internal::dynamic_library
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
    iree::third_party::hsa_runtime_headers
    loom::codegen::low::descriptors
    loom::codegen::low::frame
    loom::codegen::low::function
    loom::codegen::low::target_binding
    loom::codegen::low::text_asm
    loom::codegen::low::verify
    loom::error::diagnostic
    loom::format::text::parser
    loom::ir
    loom::ops::low
    loom::target::arch::amdgpu::amdhsa_target_id
    loom::target::arch::amdgpu::hal::binding_materialization
    loom::target::arch::amdgpu::hal::kernel_abi
    loom::target::arch::amdgpu::planning::packet_plan
    loom::target::arch::amdgpu::planning::storage_lease
    loom::target::arch::amdgpu::profile
    loom::target::arch::amdgpu::provider
    loom::target::arch::amdgpu::target_info
    loom::target::function_version
    loom::target::low_descriptor_registry
    loom::target::specialization
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "nodocker"
    "requires-gpu-amd"
    "runtime-resource=amd-gpu"
    "iree-build-requirement=loom.target.arch.amdgpu"
    "iree-build-requirement=loom.emit.amdgpu"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
