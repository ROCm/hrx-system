################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/emit/llvmir/amdgpu/BUILD.bazel                          #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR AND LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    intrinsics
  HDRS
    "intrinsics.h"
  SRCS
    "intrinsics.c"
  DEPS
    iree::base
    loom::target::emit::llvmir::core
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR AND LOOM_TARGET_ARCH_AMDGPU)
loom_cc_library(
  NAME
    target_env
  HDRS
    "target_env.h"
  SRCS
    "target_env.c"
  DEPS
    iree::base
    loom::target::emit::llvmir::target_presets
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR AND LOOM_TARGET_ARCH_AMDGPU)
loom_cc_test(
  NAME
    target_env_test
  SRCS
    "target_env_test.cc"
  DEPS
    ::target_env
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::emit::llvmir::core
    loom::target::emit::llvmir::target_env
    loom::util::stream
  LABELS
    "iree-build-requirement=loom.target.arch.llvmir"
    "iree-build-requirement=loom.emit.llvmir"
    "iree-build-requirement=loom.target.arch.amdgpu"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR AND LOOM_TARGET_ARCH_AMDGPU)
iree_execution_test_suite(
  NAME
    loom_compile_execution_test
  MANIFESTS
    "test/loom-compile.test.json"
  TOOLS
    "loom-compile=loom::tools::loom-compile"
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/target/emit/llvmir/amdgpu/test/amdgcn_kernel.loom"
  LABELS
    "hostonly"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
