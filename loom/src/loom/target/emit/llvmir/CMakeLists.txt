################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/emit/llvmir/BUILD.bazel                                 #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    core
  HDRS
    "bitcode_format.h"
    "bitcode_record_writer.h"
    "bitcode_writer.h"
    "bitstream_writer.h"
    "builder.h"
    "intrinsics_builtin.h"
    "llvmir.h"
    "module.h"
    "text_writer.h"
    "verify.h"
  SRCS
    "bitcode_record_writer.c"
    "bitcode_writer.c"
    "bitstream_writer.c"
    "builder.c"
    "intrinsics_builtin.c"
    "module.c"
    "text_writer.c"
    "types.h"
    "verify.c"
  DEPS
    iree::base
    iree::base::internal::arena
    iree::io::stream
    loom::ops::atomic
    loom::util::stream
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    target_env
  HDRS
    "target_env.h"
  SRCS
    "target_env.c"
  DEPS
    ::core
    iree::base
    loom::target::launch
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    target_presets
  HDRS
    "target_presets.h"
  SRCS
    "target_presets.c"
  DEPS
    ::target_env
    iree::base
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    module_emitter
  HDRS
    "module_emitter.h"
  SRCS
    "module_emitter.c"
  DEPS
    ::core
    ::target_env
    ::target_presets
    iree::base
    iree::base::internal::arena
    loom::analysis::symbol_facts
    loom::codegen::low::diagnostics
    loom::codegen::low::function
    loom::codegen::low::target_binding
    loom::error::emitter
    loom::error::error_defs
    loom::ir
    loom::ir::facts
    loom::ops::atomic
    loom::ops::llvmir
    loom::ops::low
    loom::target::arch::llvmir::descriptors::descriptors_ids
    loom::target::launch
    loom::target::low_descriptor_registry
    loom::target::registers
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    artifact_emitter
  HDRS
    "artifact_emitter.h"
  SRCS
    "artifact_emitter.c"
  DEPS
    ::core
    ::module_emitter
    ::target_presets
    iree::base
    iree::io::stream
    loom::target::provider
    loom::util::stream
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    test_modules_core
  HDRS
    "test_modules.h"
  SRCS
    "test_modules.c"
  DEPS
    ::core
    ::target_env
    iree::base
  DEFINES
    "LOOM_LLVMIR_TEST_MODULE_ENABLE_TARGET_SCENARIOS=0"
  TESTONLY
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    test_modules_target_scenarios
  HDRS
    "test_modules.h"
  SRCS
    "test_modules.c"
  DEPS
    ::core
    ::target_env
    iree::base
    loom::target::emit::llvmir::amdgpu::intrinsics
    loom::target::emit::llvmir::amdgpu::target_env
    loom::target::emit::llvmir::x86::intrinsics
    loom::target::emit::llvmir::x86::target_env
  TESTONLY
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    loom_check
  HDRS
    "check/loom_check.h"
  SRCS
    "check/loom_check.c"
  DEPS
    ::core
    ::module_emitter
    ::target_env
    ::target_presets
    iree::base
    iree::io::stream
    loom::codegen::low::function
    loom::ir
    loom::ops::llvmir
    loom::target::tool::llvm
    loom::tools::loom-check::execute
    loom::util::stream
  TESTONLY
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
iree_native_test(
  NAME
    "test_diagnostics"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/diagnostics.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
iree_native_test(
  NAME
    "test_emit"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/emit.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
iree_native_test(
  NAME
    "test_target_low"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/target_low.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
iree_native_test(
  NAME
    "test_toolchain"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/toolchain.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_test(
  NAME
    bitcode_record_writer_test
  SRCS
    "bitcode_record_writer_test.cc"
  DEPS
    ::core
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.llvmir"
    "iree-build-requirement=loom.emit.llvmir"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_test(
  NAME
    bitstream_writer_test
  SRCS
    "bitstream_writer_test.cc"
  DEPS
    ::core
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.llvmir"
    "iree-build-requirement=loom.emit.llvmir"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_test(
  NAME
    bitcode_writer_test
  SRCS
    "bitcode_writer_test.cc"
  DEPS
    ::core
    ::target_env
    ::test_modules_core
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.llvmir"
    "iree-build-requirement=loom.emit.llvmir"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_test(
  NAME
    bitcode_writer_target_test
  SRCS
    "bitcode_writer_test.cc"
  DEFINES
    "LOOM_LLVMIR_BITCODE_WRITER_TEST_TARGET_SCENARIOS=1"
  DEPS
    ::core
    ::target_env
    ::test_modules_target_scenarios
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.llvmir"
    "iree-build-requirement=loom.emit.llvmir"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_test(
  NAME
    text_writer_test
  SRCS
    "text_writer_test.cc"
  DEPS
    ::core
    ::test_modules_core
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.llvmir"
    "iree-build-requirement=loom.emit.llvmir"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_test(
  NAME
    text_writer_target_test
  SRCS
    "text_writer_test.cc"
  DEFINES
    "LOOM_LLVMIR_TEXT_WRITER_TEST_TARGET_SCENARIOS=1"
  DEPS
    ::core
    ::test_modules_target_scenarios
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.llvmir"
    "iree-build-requirement=loom.emit.llvmir"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_test(
  NAME
    module_emitter_test
  SRCS
    "module_emitter_test.cc"
  DEPS
    ::core
    ::module_emitter
    ::target_presets
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::text_asm
    loom::error::error_defs
    loom::format::text::parser
    loom::ir
    loom::ops::op_registry
    loom::target::arch::llvmir::descriptors::low_registry
    loom::testing::diagnostic_matchers
    loom::testing::module_ptr
    loom::util::stream
  LABELS
    "iree-build-requirement=loom.target.arch.llvmir"
    "iree-build-requirement=loom.emit.llvmir"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_test(
  NAME
    target_env_test
  SRCS
    "target_env_test.cc"
  DEPS
    ::core
    ::target_env
    ::target_presets
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::emit::llvmir::x86::target_env
  LABELS
    "iree-build-requirement=loom.target.arch.llvmir"
    "iree-build-requirement=loom.emit.llvmir"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_test(
  NAME
    tool_test
  SRCS
    "tool_test.cc"
  DEPS
    ::core
    ::test_modules_target_scenarios
    iree::io::file_handle
    iree::io::stream
    iree::testing::gtest
    iree::testing::gtest_main
    loom::target::tool::llvm
    loom::util::stream
  LABELS
    "requires-filesystem"
    "iree-build-requirement=loom.target.arch.llvmir"
    "iree-build-requirement=loom.emit.llvmir"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR AND LOOM_EMIT_LLVMIR)
loom_cc_test(
  NAME
    verify_test
  SRCS
    "verify_test.cc"
  DEPS
    ::core
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=loom.target.arch.llvmir"
    "iree-build-requirement=loom.emit.llvmir"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
