################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/all/BUILD.bazel                                         #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_TARGET_ARCH_IREE_VM AND LOOM_TARGET_ARCH_SPIRV AND LOOM_TARGET_ARCH_WASM AND LOOM_TARGET_ARCH_X86)
loom_cc_library(
  NAME
    provider
  HDRS
    "provider.h"
  SRCS
    "provider.c"
  DEPS
    iree::base
    iree::base::threading
    loom::target::arch::amdgpu::provider
    loom::target::arch::ireevm::provider
    loom::target::arch::spirv::provider
    loom::target::arch::wasm::provider
    loom::target::arch::x86::provider
    loom::target::provider
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_TARGET_ARCH_IREE_VM AND LOOM_TARGET_ARCH_SPIRV AND LOOM_TARGET_ARCH_WASM AND LOOM_TARGET_ARCH_X86)
loom_cc_library(
  NAME
    low_registry
  HDRS
    "low_registry.h"
  SRCS
    "low_registry.c"
  DEPS
    ::provider
    iree::base
    loom::codegen::low::lower
    loom::target::legalization
    loom::target::low_descriptor_registry
    loom::target::low_legality
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
