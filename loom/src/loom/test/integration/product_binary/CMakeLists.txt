################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/test/integration/product_binary/BUILD.bazel                    #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

loom_library(
  NAME
    kernel
  SRCS
    "kernel.loom"
)

loom_library(
  NAME
    kernel_bundle
  DEPS
    ::kernel
)

loom_library(
  NAME
    command
  SRCS
    "command.loom"
  DEPS
    ::kernel
    ::kernel_bundle
)

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_TARGET_AMDGPU_DESCRIPTOR_SET_GFX11_GENERIC_CORE)
loom_kernel_binary(
  NAME
    amdgpu_kernel
  DEPS
    ::command
  TARGET
    loom::target::amdgpu::gfx11-generic
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_kernel_binary(
  NAME
    spirv_kernel
  SRCS
    "kernel.loom"
  TARGET
    loom::target::spirv::vulkan1.3+bda
  FORMAT
    loom::product::formats::spirv_binary
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_command_binary(
  NAME
    spirv_command
  DEPS
    ::command
  TARGET
    loom::target::spirv::vulkan1.3+bda
)
endif()

loom_cc_binary(
  NAME
    product_test
  SRCS
    "product_test.cc"
  DEPS
    iree::base
    iree::base::internal::json
)

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_TARGET_AMDGPU_DESCRIPTOR_SET_GFX11_GENERIC_CORE)
iree_native_test(
  NAME
    "amdgpu_kernel_test"
  ARGS
    "--primary={{$<TARGET_PROPERTY:loom_test_integration_product_binary_amdgpu_kernel,IREE_GENERATED_FILE>}}"
    "--primary_magic=7f454c46"
  SRC
    ::product_test
  DATA
    "$<TARGET_PROPERTY:loom_test_integration_product_binary_amdgpu_kernel,IREE_GENERATED_FILE>"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
iree_native_test(
  NAME
    "spirv_kernel_test"
  ARGS
    "--primary={{$<TARGET_PROPERTY:loom_test_integration_product_binary_spirv_kernel,IREE_GENERATED_FILE>}}"
    "--primary_magic=03022307"
  SRC
    ::product_test
  DATA
    "$<TARGET_PROPERTY:loom_test_integration_product_binary_spirv_kernel,IREE_GENERATED_FILE>"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
  set(_SPIRV_KERNEL_TARGET
    loom_test_integration_product_binary_spirv_kernel)
  get_target_property(
    _SPIRV_KERNEL_DEPENDENCY_REPORT
    "${_SPIRV_KERNEL_TARGET}"
    LOOM_BINARY_DEPENDENCY_REPORT
  )
  iree_native_test(
    NAME spirv_dependency_component
    SRC ::product_test
    ARGS
      "--primary={{$<TARGET_PROPERTY:${_SPIRV_KERNEL_TARGET},IREE_GENERATED_FILE>}}"
      "--primary_magic=03022307"
      "--dependency_report={{${_SPIRV_KERNEL_DEPENDENCY_REPORT}}}"
      "--dependency_component=//loom/src/loom/test/integration/product_binary:spirv_kernel"
  )

  set(_COMMAND_TARGET loom_test_integration_product_binary_spirv_command)
  get_target_property(
    _MANIFEST_FILE "${_COMMAND_TARGET}" LOOM_BINARY_FILE)
  get_target_property(
    _ARTIFACT_DIRECTORY
    "${_COMMAND_TARGET}"
    LOOM_BINARY_COMMAND_ARTIFACT_DIRECTORY
  )
  get_target_property(
    _KERNEL_FILE "${_COMMAND_TARGET}" LOOM_BINARY_KERNEL_FILE)
  iree_native_test(
    NAME spirv_command
    SRC ::product_test
    ARGS
      "--primary={{${_MANIFEST_FILE}}}"
      "--manifest={{${_MANIFEST_FILE}}}"
      "--artifacts={{${_ARTIFACT_DIRECTORY}}}"
      "--kernel={{${_KERNEL_FILE}}}"
      "--kernel_magic=03022307"
  )
endif()
