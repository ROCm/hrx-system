################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/test/corpus/authoring/BUILD.bazel                              #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
iree_native_test(
  NAME
    "ffn_gate_up_swiglu_q6q8_plan_test"
  ARGS
    "{{${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/authoring/ffn_gate_up_swiglu_q6q8.loom}}"
    "--dry-run"
  SRC
    loom::tools::iree-benchmark-loom
  LABELS
    "hostonly"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
iree_native_test(
  NAME
    "indexed_row_gather_f32_plan_test"
  ARGS
    "{{${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/authoring/indexed_row_gather_f32.loom}}"
    "--dry-run"
  SRC
    loom::tools::iree-benchmark-loom
  LABELS
    "hostonly"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
iree_native_test(
  NAME
    "memset_i8_plan_test"
  ARGS
    "{{${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/authoring/memset_i8.loom}}"
    "--dry-run"
  SRC
    loom::tools::iree-benchmark-loom
  LABELS
    "hostonly"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
iree_native_test(
  NAME
    "mlp_down_projection_residual_bf16_plan_test"
  ARGS
    "{{${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/authoring/mlp_down_projection_residual_bf16.loom}}"
    "--dry-run"
  SRC
    loom::tools::iree-benchmark-loom
  LABELS
    "hostonly"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_AMDGPU)
iree_native_test(
  NAME
    "ffn_gate_up_swiglu_q6q8_amdgpu_test"
  ARGS
    "{{${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/authoring/ffn_gate_up_swiglu_q6q8.loom}}"
    "--device=amdgpu"
    "--measure=dispatch_complete"
    "--iterations=1"
    "--warmup-iterations=0"
    "--batch-size=1"
    "--min-time-ms=0"
    "--max-batches=1"
    "--input-ring-count=1"
  SRC
    loom::tools::iree-benchmark-loom
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "manual"
    "requires-gpu-amd"
    "runtime-resource=amd-gpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_AMDGPU)
iree_native_test(
  NAME
    "indexed_row_gather_f32_amdgpu_test"
  ARGS
    "{{${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/authoring/indexed_row_gather_f32.loom}}"
    "--device=amdgpu"
    "--measure=dispatch_complete"
    "--sample-compilation=per_sample"
    "--iterations=1"
    "--warmup-iterations=0"
    "--batch-size=1"
    "--min-time-ms=0"
    "--max-batches=1"
    "--input-ring-count=1"
  SRC
    loom::tools::iree-benchmark-loom
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "manual"
    "requires-gpu-amd"
    "runtime-resource=amd-gpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_AMDGPU)
iree_native_test(
  NAME
    "memset_i8_amdgpu_test"
  ARGS
    "{{${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/authoring/memset_i8.loom}}"
    "--device=amdgpu"
    "--measure=dispatch_complete"
    "--sample-compilation=per_sample"
    "--iterations=1"
    "--warmup-iterations=0"
    "--batch-size=1"
    "--min-time-ms=0"
    "--max-batches=1"
    "--input-ring-count=1"
  SRC
    loom::tools::iree-benchmark-loom
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "manual"
    "requires-gpu-amd"
    "runtime-resource=amd-gpu"
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_AMDGPU)
iree_native_test(
  NAME
    "mlp_down_projection_residual_bf16_amdgpu_test"
  ARGS
    "{{${PROJECT_SOURCE_DIR}/loom/src/loom/test/corpus/authoring/mlp_down_projection_residual_bf16.loom}}"
    "--device=amdgpu"
    "--measure=dispatch_complete"
    "--sample-compilation=per_sample"
    "--iterations=1"
    "--warmup-iterations=0"
    "--batch-size=1"
    "--min-time-ms=0"
    "--max-batches=1"
    "--input-ring-count=1"
  SRC
    loom::tools::iree-benchmark-loom
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "manual"
    "requires-gpu-amd"
    "runtime-resource=amd-gpu"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
