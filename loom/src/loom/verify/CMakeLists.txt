################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/verify/BUILD.bazel                                             #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

loom_cc_library(
  NAME
    verify
  HDRS
    "verify.h"
    "verify_constraints.h"
    "verify_diagnostics.h"
    "verify_ownership.h"
    "verify_state.h"
    "verify_structure.h"
  SRCS
    "verify.c"
    "verify_constraints.c"
    "verify_diagnostics.c"
    "verify_ownership.c"
    "verify_state.c"
    "verify_structure.c"
  DEPS
    iree::base
    iree::base::internal::arena
    iree::base::internal::unicode
    loom::analysis::consumption
    loom::error::diagnostic
    loom::error::emitter
    loom::error::error_defs
    loom::error::renderer
    loom::format::text::printer
    loom::ir
    loom::ops::op_defs
    loom::ops::special_values
    loom::target::registers
  PUBLIC
)

loom_cc_test(
  NAME
    verify_test
  SRCS
    "verify_test.cc"
  DEPS
    ::verify
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::error::diagnostic
    loom::error::error_defs
    loom::error::json_sink
    loom::error::renderer
    loom::format::text::parser
    loom::ir
    loom::ops::low
    loom::ops::op_defs
    loom::ops::test
    loom::testing::diagnostic_matchers
    loom::util::stream
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
