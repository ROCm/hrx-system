################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tools/loom-check/BUILD.bazel                                   #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

loom_cc_library(
  NAME
    target_low_registry_manifest
  HDRS
    "target_low_registry_manifest.h"
  SRCS
    "target_low_registry_manifest.c"
  DEPS
    iree::base
    loom::codegen::low::descriptors
    loom::target::low_descriptor_registry
    loom::util::json
    loom::util::stream
  PUBLIC
)

loom_cc_library(
  NAME
    execute
  HDRS
    "comparison.h"
    "diagnostics.h"
    "execute.h"
    "input.h"
    "low_emit.h"
    "low_report.h"
    "requirements.h"
    "source_low.h"
  SRCS
    "comparison.c"
    "diagnostics.c"
    "emit.c"
    "execute.c"
    "input.c"
    "low_emit.c"
    "low_report.c"
    "requirements.c"
    "roundtrip.c"
    "source_low.c"
    "verify.c"
  DEPS
    ::report
    ::target_low_registry_manifest
    ::update
    iree::base
    iree::base::internal::arena
    iree::io::file_handle
    iree::io::stream
    loom::analysis::liveness
    loom::analysis::liveness_json
    loom::analysis::pipeline_plan
    loom::codegen::low::allocation
    loom::codegen::low::allocation_json
    loom::codegen::low::descriptors
    loom::codegen::low::diagnostics
    loom::codegen::low::frame
    loom::codegen::low::function
    loom::codegen::low::function_model
    loom::codegen::low::lower
    loom::codegen::low::packet_json
    loom::codegen::low::pipeline::legalizer_registry
    loom::codegen::low::pipeline::pass_environment
    loom::codegen::low::schedule
    loom::codegen::low::target_binding
    loom::codegen::low::text_asm
    loom::codegen::low::verify
    loom::error::diagnostic
    loom::error::emitter
    loom::error::error_defs
    loom::error::json_sink
    loom::error::renderer
    loom::error::source
    loom::format::text::parser
    loom::format::text::printer
    loom::ir
    loom::ops::func
    loom::ops::kernel
    loom::ops::low
    loom::ops::op_defs
    loom::ops::pipeline
    loom::ops::target
    loom::pass::builtin_registry
    loom::pass::pipeline
    loom::pass::registry
    loom::pass::report
    loom::pass::tooling
    loom::sanitizer::options
    loom::target::entry_selection
    loom::target::function_version_projection
    loom::target::legalization
    loom::target::low_asm_diagnostics
    loom::target::low_descriptor_registry
    loom::target::low_legality
    loom::target::low_packet_diagnostics
    loom::target::math_policy
    loom::target::pipeline
    loom::target::predicate
    loom::target::reporting::format
    loom::target::reporting::low
    loom::target::selection
    loom::target::types
    loom::testing::test_diagnostic
    loom::testing::test_file
    loom::tooling::compile::pipeline
    loom::tooling::input
    loom::tools::loom-format::convert
    loom::util::diff
    loom::util::fact_table
    loom::util::json
    loom::util::stream
    loom::verify
  PUBLIC
)

loom_cc_library(
  NAME
    test_util
  HDRS
    "test_util.h"
  SRCS
    "test_util.cc"
  DEPS
    ::execute
    ::report
    iree::base
    iree::base::internal::arena
    loom::ir
    loom::testing::test_file
  TESTONLY
  PUBLIC
)

loom_cc_library(
  NAME
    report
  HDRS
    "report.h"
  SRCS
    "report.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::testing::test_file
  PUBLIC
)

loom_cc_test(
  NAME
    execute_test
  SRCS
    "execute_test.cc"
  DEPS
    ::execute
    ::report
    iree::base
    iree::base::internal::arena
    iree::base::internal::json
    iree::testing::gtest
    iree::testing::gtest_main
    loom::error::error_defs
    loom::ir
    loom::target::low_descriptor_registry_core_test
    loom::target::provider
    loom::target::test::lower
    loom::testing::context
    loom::testing::test_file
)

loom_cc_library(
  NAME
    update
  HDRS
    "update.h"
  SRCS
    "update.c"
  DEPS
    iree::base
    loom::testing::test_file
  PUBLIC
)

loom_cc_library(
  NAME
    template_sync
  HDRS
    "template_sync.h"
  SRCS
    "template_sync.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::format::text::parser
    loom::ir
    loom::ops::op_defs
    loom::testing::test_file
  PUBLIC
)

loom_cc_test(
  NAME
    update_test
  SRCS
    "update_test.cc"
  DEPS
    ::update
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::testing::test_file
)

loom_cc_test(
  NAME
    template_sync_test
  SRCS
    "template_sync_test.cc"
  DEPS
    ::template_sync
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::ir
    loom::ops::config
    loom::ops::func
    loom::ops::index
    loom::ops::kernel
    loom::ops::pipeline
    loom::ops::scalar
    loom::testing::test_file
)

loom_cc_library(
  NAME
    json_output
  HDRS
    "json_output.h"
  SRCS
    "json_output.c"
  DEPS
    ::execute
    ::report
    ::update
    iree::base
    loom::testing::test_file
    loom::util::json
    loom::util::stream
  PUBLIC
)

loom_cc_library(
  NAME
    output
  HDRS
    "output.h"
  SRCS
    "output.c"
  DEPS
    ::execute
    iree::base
    iree::base::core_headers
    loom::testing::test_file
  PUBLIC
)

loom_cc_library(
  NAME
    compile
  HDRS
    "compile.h"
  SRCS
    "compile.c"
  DEPS
    ::execute
    iree::base
    loom::codegen::low::repr
    loom::link::linker
    loom::target::entry_selection
    loom::tooling::compile::configured
    loom::tooling::compile::preparation
    loom::tooling::config
  PUBLIC
)

loom_cc_library(
  NAME
    file
  HDRS
    "file.h"
  SRCS
    "file.c"
  DEPS
    ::compile
    ::execute
    ::json_output
    ::output
    ::report
    ::template_sync
    ::update
    iree::base
    iree::base::internal::arena
    iree::io::file_handle
    loom::codegen::low::text_asm
    loom::ir
    loom::testing::test_file
    loom::testing::test_file_format
    loom::tooling::io::file
    loom::tooling::io::source_path
    loom::util::stream
  PUBLIC
)

loom_cc_test(
  NAME
    file_test
  SRCS
    "file_test.cc"
  DEPS
    ::file
    iree::base
    iree::base::internal::arena
    iree::io::file_handle
    iree::testing::gtest
    iree::testing::gtest_main
    iree::testing::temp_file
    loom::ir
    loom::testing::context
)

loom_cc_library(
  NAME
    main
  HDRS
    "main.h"
  SRCS
    "main.c"
  DEPS
    ::execute
    ::file
    ::json_output
    ::output
    iree::base
    iree::base::internal::arena
    iree::base::tooling::flags
    loom::sanitizer::options
    loom::tooling::cli::help
    loom::tooling::compile::configured
    loom::tooling::config
    loom::tooling::context
  PUBLIC
)

loom_cc_library(
  NAME
    provider
  HDRS
    "provider.h"
  SRCS
    "provider.c"
  DEPS
    ::main
    iree::base
    loom::target::provider
    loom::tooling::context
    loom::tooling::input
  PUBLIC
)

loom_cc_library(
  NAME
    test_provider
  HDRS
    "test_provider.h"
  SRCS
    "test_provider.c"
  DEPS
    ::execute
    ::provider
    iree::base
    loom::codegen::low::allocation
    loom::codegen::low::packet_hazard_plan
    loom::codegen::low::packet_hazard_plan_json
    loom::codegen::low::packet_progress
    loom::ir
    loom::target::test::provider
  TESTONLY
  PUBLIC
)

loom_cc_test(
  NAME
    json_output_test
  SRCS
    "json_output_test.cc"
  DEPS
    ::execute
    ::json_output
    ::report
    iree::base
    iree::base::internal::arena
    iree::base::internal::json
    iree::testing::gtest
    iree::testing::gtest_main
    loom::testing::test_file
    loom::util::stream
)

set(_loom_check_platform_copts "")
if(LOOM_EMIT_AMDGPU)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_EMIT_AMDGPU=1")
endif()
if(LOOM_TARGET_ARCH_SPIRV)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_TARGET_SPIRV=1")
endif()
if(LOOM_EMIT_SPIRV)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_EMIT_SPIRV=1")
endif()
if(LOOM_TARGET_ARCH_WASM)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_TARGET_WASM=1")
endif()
if(LOOM_TARGET_ARCH_VM)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_TARGET_VM=1")
endif()
if(LOOM_EMIT_WASM)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_EMIT_WASM=1")
endif()
if(LOOM_TARGET_ARCH_X86)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_TARGET_X86=1")
endif()
if(LOOM_TARGET_ARCH_XDNA)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_TARGET_XDNA=1")
endif()
set(_loom_check_platform_deps "")
if(LOOM_EMIT_AMDGPU)
  list(APPEND _loom_check_platform_deps loom::target::arch::amdgpu::check::provider)
endif()
if(LOOM_TARGET_ARCH_SPIRV)
  list(APPEND _loom_check_platform_deps loom::target::arch::spirv::check::check_provider)
endif()
if(LOOM_EMIT_SPIRV)
  list(APPEND _loom_check_platform_deps loom::target::emit::spirv::check_provider)
endif()
if(LOOM_TARGET_ARCH_WASM)
  list(APPEND _loom_check_platform_deps loom::target::arch::wasm::check::check_provider)
endif()
if(LOOM_TARGET_ARCH_VM)
  list(APPEND _loom_check_platform_deps loom::target::arch::vm::check)
endif()
if(LOOM_EMIT_WASM)
  list(APPEND _loom_check_platform_deps loom::target::emit::wasm::check_provider)
endif()
if(LOOM_TARGET_ARCH_X86)
  list(APPEND _loom_check_platform_deps loom::target::arch::x86::check::check_provider)
endif()
if(LOOM_TARGET_ARCH_XDNA)
  list(APPEND _loom_check_platform_deps loom::target::arch::amd::xdna::aie2p::check_provider)
endif()
loom_cc_binary(
  NAME
    loom-check
  SRCS
    "loom-check.c"
  COPTS
    ${_loom_check_platform_copts}
  DEPS
    ::provider
    iree::base
    loom::target::arch::cmd::check::check_provider
    loom::tooling::input::configured
    ${_loom_check_platform_deps}
)

set(_loom_check_test_platform_copts "")
if(LOOM_EMIT_AMDGPU)
  list(APPEND _loom_check_test_platform_copts "-DLOOM_CHECK_HAVE_EMIT_AMDGPU=1")
endif()
if(LOOM_TARGET_ARCH_SPIRV)
  list(APPEND _loom_check_test_platform_copts "-DLOOM_CHECK_HAVE_TARGET_SPIRV=1")
endif()
if(LOOM_EMIT_SPIRV)
  list(APPEND _loom_check_test_platform_copts "-DLOOM_CHECK_HAVE_EMIT_SPIRV=1")
endif()
if(LOOM_TARGET_ARCH_WASM)
  list(APPEND _loom_check_test_platform_copts "-DLOOM_CHECK_HAVE_TARGET_WASM=1")
endif()
if(LOOM_TARGET_ARCH_VM)
  list(APPEND _loom_check_test_platform_copts "-DLOOM_CHECK_HAVE_TARGET_VM=1")
endif()
if(LOOM_EMIT_WASM)
  list(APPEND _loom_check_test_platform_copts "-DLOOM_CHECK_HAVE_EMIT_WASM=1")
endif()
if(LOOM_TARGET_ARCH_X86)
  list(APPEND _loom_check_test_platform_copts "-DLOOM_CHECK_HAVE_TARGET_X86=1")
endif()
if(LOOM_TARGET_ARCH_XDNA)
  list(APPEND _loom_check_test_platform_copts "-DLOOM_CHECK_HAVE_TARGET_XDNA=1")
endif()
set(_loom_check_test_platform_deps "")
if(LOOM_EMIT_AMDGPU)
  list(APPEND _loom_check_test_platform_deps loom::target::arch::amdgpu::check::provider)
endif()
if(LOOM_TARGET_ARCH_SPIRV)
  list(APPEND _loom_check_test_platform_deps loom::target::arch::spirv::check::check_provider)
endif()
if(LOOM_EMIT_SPIRV)
  list(APPEND _loom_check_test_platform_deps loom::target::emit::spirv::check_provider)
endif()
if(LOOM_TARGET_ARCH_WASM)
  list(APPEND _loom_check_test_platform_deps loom::target::arch::wasm::check::check_provider)
endif()
if(LOOM_TARGET_ARCH_VM)
  list(APPEND _loom_check_test_platform_deps loom::target::arch::vm::check)
endif()
if(LOOM_EMIT_WASM)
  list(APPEND _loom_check_test_platform_deps loom::target::emit::wasm::check_provider)
endif()
if(LOOM_TARGET_ARCH_X86)
  list(APPEND _loom_check_test_platform_deps loom::target::arch::x86::check::check_provider)
endif()
if(LOOM_TARGET_ARCH_XDNA)
  list(APPEND _loom_check_test_platform_deps loom::target::arch::amd::xdna::aie2p::check_provider)
endif()
loom_cc_binary(
  NAME
    loom-check-test
  SRCS
    "loom-check.c"
  COPTS
    "-DLOOM_CHECK_HAVE_TEST_PROVIDER=1"
    ${_loom_check_test_platform_copts}
  DEPS
    ::provider
    ::test_provider
    iree::base
    loom::target::arch::cmd::check::check_provider
    loom::tooling::input::configured
    ${_loom_check_test_platform_deps}
  TESTONLY
)

loom_cc_test(
  NAME
    comparison_test
  SRCS
    "comparison_test.cc"
  DEPS
    ::execute
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
    loom::testing::test_file
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
