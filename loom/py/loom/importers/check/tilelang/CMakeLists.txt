################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/py/loom/importers/check/tilelang/BUILD.bazel                            #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

iree_py_library(
  NAME
    tilelang
  SRCS
    "__init__.py"
    "backend.py"
    "cases.py"
    "harness.py"
    "runner.py"
  DEPS
    loom::py::loom::importers::check::check
    loom::py::loom::importers::core::core
    loom::py::loom::importers::tilelang::tilelang
    loom::py::loom::importers::tilelang::oracle
)

if(LOOM_IMPORT_TILELANG)

if(LOOM_IMPORT_TILELANG)
iree_py_test(
  NAME
    tilelang_test
  SRCS
    "../../../tools/pytest_style_runner.py"
  ARGS
    "loom.importers.check.tilelang.cases_test"
    "loom.importers.check.tilelang.harness_test"
    "loom.importers.check.tilelang.runner_test"
  DEPS
    loom::py::loom::importers::check::tilelang::tilelang
    loom::py::loom::importers::tilelang::tilelang_packages
  PACKAGE_DIRS
    "${PROJECT_SOURCE_DIR}/loom/py"
    "${PROJECT_BINARY_DIR}/loom/py"
)
endif()

endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
