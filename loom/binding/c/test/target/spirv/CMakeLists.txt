################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/binding/c/test/target/spirv/BUILD.bazel                                 #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    profile_test
  SRCS
    "profile_test.cc"
  DEPS
    iree::testing::gtest
    iree::testing::gtest_main
    loom::binding::c::loomc
    loom::binding::c::target::spirv
    loom::binding::c::test::test_util
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    vulkaninfo_test
  SRCS
    "vulkaninfo_test.cc"
  DEPS
    iree::testing::gtest
    iree::testing::gtest_main
    iree::testing::temp_file
    loom::binding::c::loomc
    loom::binding::c::target::spirv
    loom::binding::c::target::spirv::vulkaninfo
    loom::binding::c::test::test_util
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
set(_vulkan_test_platform_deps "")
if(LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
  list(APPEND _vulkan_test_platform_deps iree::third_party::vulkan_headers)
endif()
loom_cc_test(
  NAME
    vulkan_test
  SRCS
    "vulkan_test.cc"
  DEPS
    iree::testing::gtest
    iree::testing::gtest_main
    loom::binding::c::loomc
    loom::binding::c::target::spirv
    loom::binding::c::target::spirv::vulkan
    loom::binding::c::test::test_util
    ${_vulkan_test_platform_deps}
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    iree_hal_test
  SRCS
    "iree_hal_test.cc"
  DEPS
    iree::hal
    iree::testing::gtest
    iree::testing::gtest_main
    loom::binding::c::loomc
    loom::binding::c::target::iree_hal
    loom::binding::c::target::spirv
    loom::binding::c::target::spirv::iree_hal
    loom::binding::c::test::test_util
  LABELS
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
)
endif()

if(LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN AND LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_test(
  NAME
    iree_hal_execution_test
  SRCS
    "iree_hal_execution_test.cc"
  DEPS
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
    loom::binding::c::loomc
    loom::binding::c::target::iree_hal
    loom::binding::c::target::spirv
    loom::binding::c::target::spirv::iree_hal
    loom::binding::c::test::target::iree_hal_execution_test_util
  LABELS
    "iree-run-requirement=runtime.resource.vulkan_device"
    "runtime-resource=vulkan-device"
    "iree-build-requirement=loom.target.arch.spirv"
    "iree-build-requirement=loom.emit.spirv"
  SANITIZER_SUPPRESSIONS
    lsan
    vulkan
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
