amdgpu.target<gfx1151> @gfx1151

kernel.def target(@gfx1151) @non_scalar_workload(%input: buffer) {
  %c1 = index.constant 1 : index
  kernel.launch.config workgroups(%c1, %c1, %c1) workgroup_size(%c1, %c1, %c1) : index
} launch() {
  kernel.return
}
