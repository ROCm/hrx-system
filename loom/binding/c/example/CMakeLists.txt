################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/binding/c/example/BUILD.bazel                                           #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

loom_cc_binary(
  NAME
    source_info
  SRCS
    "source_info.c"
  DEPS
    loom::binding::c::loomc
)

loom_cc_binary(
  NAME
    compile_text
  SRCS
    "compile_text.c"
  DEPS
    loom::binding::c::loomc
)

loom_cc_binary(
  NAME
    link_modules
  SRCS
    "link_modules.c"
  DEPS
    loom::binding::c::loomc
)

iree_native_test(
  NAME
    "link_modules_test"
  SRC
    ::link_modules
)

loom_cc_binary(
  NAME
    product_frontier
  SRCS
    "product_frontier.c"
  DEPS
    loom::binding::c::loomc
    loom::binding::c::target::cmd
    loom::binding::c::target::kernel
)

iree_native_test(
  NAME
    "product_frontier_test"
  SRC
    ::product_frontier
)

if(IREE_TARGET_HAS_THREADS)

loom_cc_binary(
  NAME
    jit_task_pool
  SRCS
    "jit_task_pool.c"
  DEPS
    loom::binding::c::loomc
    loom::binding::c::task_pool
    loom::binding::c::task_queue
)

iree_native_test(
  NAME
    "jit_task_pool_test"
  SRC
    ::jit_task_pool
)

endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_binary(
  NAME
    emit_spirv_offline
  SRCS
    "emit_spirv_offline.c"
  DEPS
    loom::binding::c::loomc
    loom::binding::c::target::spirv
)
endif()

loom_module(
  NAME
    targetless_store_i32_bytecode
  SRCS
    "targetless_store_i32.loom"
  MODE
    "merge"
  OUTPUT
    "targetless_store_i32_bytecode.loombc"
  OUTPUT_FORMAT
    "bc"
)

iree_c_embed_data(
  NAME
    targetless_store_i32_bytecode_data
  SRCS
    "${CMAKE_CURRENT_BINARY_DIR}/targetless_store_i32_bytecode.loombc"
  C_FILE_OUTPUT
    "targetless_store_i32_bytecode.c"
  H_FILE_OUTPUT
    "targetless_store_i32_bytecode.h"
  IDENTIFIER
    "loomc_example_targetless_store_i32_bytecode"
  FLATTEN
  PUBLIC
)

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_binary(
  NAME
    emit_amdgpu_offline
  SRCS
    "emit_amdgpu_offline.c"
  DEPS
    ::targetless_store_i32_bytecode_data
    loom::binding::c::loomc
    loom::binding::c::target::amdgpu
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_binary(
  NAME
    emit_amdgpu_hsa
  SRCS
    "emit_amdgpu_hsa.c"
  DEPS
    ${CMAKE_DL_LIBS}
    iree::third_party::hsa_runtime_headers
    loom::binding::c::loomc
    loom::binding::c::target::amdgpu
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV)
loom_cc_binary(
  NAME
    emit_spirv_vulkaninfo
  SRCS
    "emit_spirv_vulkaninfo.c"
  DEPS
    loom::binding::c::loomc
    loom::binding::c::target::spirv
    loom::binding::c::target::spirv::vulkaninfo
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
loom_cc_binary(
  NAME
    emit_spirv_vulkan
  SRCS
    "emit_spirv_vulkan.c"
  DEPS
    ${CMAKE_DL_LIBS}
    loom::binding::c::loomc
    loom::binding::c::target::spirv
    loom::binding::c::target::spirv::vulkan
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND IREE_HAL_DRIVER_VULKAN)
loom_cc_binary(
  NAME
    emit_spirv_iree_hal
  SRCS
    "emit_spirv_iree_hal.c"
  DEPS
    iree::async::util::proactor_pool
    iree::base
    iree::base::threading
    iree::hal
    iree::hal::drivers
    loom::binding::c::loomc
    loom::binding::c::target::iree_hal
    loom::binding::c::target::spirv
    loom::binding::c::target::spirv::iree_hal
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
iree_native_test(
  NAME
    "emit_amdgpu_offline_test"
  SRC
    ::emit_amdgpu_offline
)
endif()

if(LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU)
loom_cc_test(
  NAME
    emit_amdgpu_hsa_test
  SRCS
    "emit_amdgpu_hsa.c"
  DEPS
    ${CMAKE_DL_LIBS}
    iree::third_party::hsa_runtime_headers
    loom::binding::c::loomc
    loom::binding::c::target::amdgpu
  LABELS
    "driver=amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "nodocker"
    "requires-gpu-amd"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    loom-amdgpu-tests
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
loom_cc_test(
  NAME
    emit_spirv_vulkan_test
  SRCS
    "emit_spirv_vulkan.c"
  DEPS
    ${CMAKE_DL_LIBS}
    loom::binding::c::loomc
    loom::binding::c::target::spirv
    loom::binding::c::target::spirv::vulkan
  LABELS
    "driver=vulkan"
    "iree-run-requirement=runtime.resource.vulkan_device"
    "runtime-resource=vulkan-device"
  RESOURCE_GROUP
    loom-vulkan-tests
  SANITIZER_SUPPRESSIONS
    lsan
    vulkan
)
endif()

if(LOOM_TARGET_ARCH_SPIRV AND LOOM_EMIT_SPIRV AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_VULKAN)
loom_cc_test(
  NAME
    emit_spirv_iree_hal_test
  SRCS
    "emit_spirv_iree_hal.c"
  DEPS
    iree::async::util::proactor_pool
    iree::base
    iree::base::threading
    iree::hal
    iree::hal::drivers
    loom::binding::c::loomc
    loom::binding::c::target::iree_hal
    loom::binding::c::target::spirv
    loom::binding::c::target::spirv::iree_hal
  LABELS
    "driver=vulkan"
    "iree-run-requirement=runtime.resource.vulkan_device"
    "runtime-resource=vulkan-device"
  RESOURCE_GROUP
    loom-vulkan-tests
  SANITIZER_SUPPRESSIONS
    lsan
    vulkan
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
