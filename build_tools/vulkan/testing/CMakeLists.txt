################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# build_tools/vulkan/testing/BUILD.bazel                                       #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(IREE_VULKAN_AVAILABLE)
iree_cc_library(
  NAME
    context
  HDRS
    "context.h"
  SRCS
    "context.cc"
  DEPS
    ${CMAKE_DL_LIBS}
    iree::third_party::google_test
    iree::third_party::vulkan_headers
  TESTONLY
  PUBLIC
)
endif()

if(IREE_VULKAN_AVAILABLE)
iree_cc_library(
  NAME
    buffer
  HDRS
    "buffer.h"
  SRCS
    "buffer.cc"
  DEPS
    ::context
    iree::third_party::google_test
    iree::third_party::vulkan_headers
  TESTONLY
  PUBLIC
)
endif()

if(IREE_VULKAN_AVAILABLE)
iree_cc_library(
  NAME
    compute
  HDRS
    "compute.h"
  SRCS
    "compute.cc"
  DEPS
    ::context
    iree::third_party::google_test
    iree::third_party::vulkan_headers
  TESTONLY
  PUBLIC
)
endif()

if(IREE_VULKAN_AVAILABLE)
iree_cc_library(
  NAME
    submission
  HDRS
    "submission.h"
  SRCS
    "submission.cc"
  DEPS
    ::context
    iree::third_party::google_test
    iree::third_party::vulkan_headers
  TESTONLY
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
